# Auditoría pre-lanzamiento — VisualSearch (visual_search)

**Fecha:** 2026-09-12
**Auditor:** Hermes (FASE B.5, KRU-116)
**Estado:** **✅ PASS — P1×3 y P2×5 cerrados en esta card (RED→GREEN + smoke E2E); P3×5 cerrados + P3×3 abiertos sistemáticos + P3×2 decisionados**
**Rollup:** `docs/qa/prelaunch-rollup.md` (fila 5)
**Doc módulo R-6:** `docs/design/modulos/visual_search.md` (creada en esta card — 5.º y último de stable_dg)

---

## Contexto

- Batería: `stable_dg` — bloque **visible** (juego 4 de 4 en "Juego X de N"), `skill: 'visual_search_efficiency'`, `trialCount: 4`, `durationLabel: '1 min'` → **fix a `'45 s'`** (VSP-P3-1: 4 panels × (RT ~1–3 s + ITI) ≈ 6–14 s típicos; peor caso 4 timeouts × 10.4 s ≈ 42 s). En `batteryConfig.js` (assessment) también existe con `trialCount: 12` (eval: 3× cada set size) / 4 (practice) — flujo legacy, sin cambios (mismo tratamiento que B.1–B.4).
- Código: `src/tasks/VisualSearchTask.jsx` (299→424 líneas). Tests: `src/tasks/visualSearch.test.jsx` (160→500 líneas; 8→**20 tests**). Tocados además: `postulationDemoConfig.js` (+test), `postulationDemo.css` (feedback chip + floor 44 + brief retirado), `PostulationGamesDesignSystem.test.jsx` (aserto CSS re-apuntado).
- Render en batería: `PostulationGameStage` pasa `width/height` desde `getPostulationGameViewport()` (**520×290** @1280×720 y **312×340** @390×844 — verificado en vivo; piso 240×280) + `trialCount` del bloque (4).
- Mecánica: array de formas geométricas (1 objetivo ● entre ○/◇/□/△); clic/tap en el objetivo; rampa de set size 8→12→16→20 (1 panel por tamaño en batería); timeout por trial 10 s (fix VSP-P1-1); ITI 350 ms con jitter (fix VSP-P2-5).

## Veredicto por dimensión (8)

| # | Dimensión | Veredicto | Evidencia |
|---|-----------|-----------|-----------|
| 1 | Toma de muestras | ✅ PASS (1 P2 cerrado) | 1 respuesta por panel (click discreto; sin pointer sampler — note (f) N/A, como B.3/B.4). Clock único `performance.now()` (pitfall 23 ✓; `startTimeRef` en el effect → sesgo de montaje ~1 frame, sub-segundo, documentado en doc §7). Reset por panel (`handledRef`/feedback/`startTimeRef`). **P2-1 (cerrado):** el summary no llevaba las claves de contrato del session builder (`completedTrialCount`, `meanReactionTimeMs`, `meanScore`) → la RT de visual_search no entraba a la media de batería y la card del reporte no mostraba **"Puntaje"** (`pushMetric` skip de null/'—'; mismo bug GNP-P2-1/CIP-P2-1). Fix: keys añadidas + test de contrato + **E2E en smoke: card VS en reporte vivo con Puntaje 75%/100% (desktop/móvil)**. Calidad: todo panel termina (click o timeout — P1-1) → denominador completo. |
| 2 | Telemetría | ✅ PASS (2 P2 + 2 P3 cerrados) | Eventos: `game_start` ✓ (GameRuntime), `stimulus_shown` ✓ (setSize/**targetIndex**/distractorCount/targetSymbol — metadata), `response` ✓ (outcome, RT, score, bloque `visualSearch` + **`pointer` real** — fix VSP-P2-2), **`game_end` ✓ — verificado (note (d)): ya se emitía** (rama de completado, antes de `onComplete`); se normalizó escala 0-1→0-100 (VSP-P3-2) + difficulty (VSP-P3-3). Contrato `game_event_v1` vía `normalizeGameEvent` ✓. Privacidad: `trials` del summary **se elimina** en `stripForbidden` (`ASSESSMENT_FORBIDDEN_KEYS` incluye `trials`/`items`) + re-validación recursiva en `validateFinalAssessmentPayload` ✓ (suite focal: session builder 7/7 + finalAssessmentPayload 2/2). `pointer` = **1 punto por respuesta** (allowlist del contrato; `sanitizePointer`), sin trayectoria. **P2-2 (cerrado):** localización degenerada — `pointer` era el centro de la **celda** (`event` sin usar) y `clickDistanceToTargetPx` era distancia celda-celda (0 en acierto, siempre). Fix: coords reales del evento relativas al canvas (guard `detail>0` + finitud), fallback teclado al centro del tile, distancia euclídea real; null en timeout; 2 tests (mouse + teclado). Nota sistémica (documentada, sin fix): effect hijo antes que padre → `stimulus_shown` precede a `game_start` en TODOS los juegos GameRuntime (B.3) — los tests no asertan orden entre ambos. |
| 3 | Sincronización | ✅ PASS (1 P1 + 1 P2 + 2 P3 cerrados) | Máquina: panel N (stimulus_shown, ventana 10 s) → click/timeout → feedback + ITI → panel N+1; **todo panel termina** (fix VSP-P1-1) → sin estados irresolubles/hang. **P1-2 (cerrado):** geometría dependiente de viewport mid-trial — `useMemo([width, height, count])` (línea 138 del legado, flaggeada en el rollup B.2→B.5) + effect dependiente de la identidad del trial → el stage re-mide en `resize`/`visualViewport` (muy común en móvil) y **reseteaba el panel en curso** (estímulo huérfano + RT reiniciada). Fix: `initialSizeRef` (patrón B.2) — trials y task-area usan el tamaño bloqueado al montar; test (re-render con size nuevo: sin re-emisión, canvas `width: 600px` intacto) + inamovible en smoke. **P2-5 (cerrado):** ITI `setTimeout` sin ref/cleanup (onComplete tardío tras abort) + ritmo fijo → `itiRef` + cleanup + jitter [0.75,1.25] (test de límites vía delay registrado; test unmount-during-ITI). **P3-4 (cerrado):** StrictMode duplicaba `stimulus_shown` del panel 0 → guard `emittedForRef` + test. **P3-5 (cerrado):** sin `handledRef` (doble-tap → 2 responses + trial duplicado en summary) → guard + test de doble-click. |
| 4 | Gameplay | ✅ PASS (2 P2 cerrados) | Reglas según diseño: 1 objetivo ● entre N-1 distractores, clic en el objetivo. Edge cases: **timeout** (fix P1-1: `outcome:'timeout'`, correct:false, feedback "Tiempo agotado" — E2E en smoke), **doble-click** (handledRef), **foco perdido** (el timeout corre igual → el panel termina), **abandono** (abort del flujo). Práctica: sin botón de práctica en stable_dg (micro-bloques; OUT documentado en doc §1.3, como B.3/B.4); el panel 1 (size 8) es warm-up implícito. Dificultad: rampa set size 8→12→16→20 (diseño `set_size`). **P2-4 (cerrado):** arrays **deterministas** por sesión (`targetIndex = (5i+3)%size`, símbolo `(i+trial)%4`) → mismos panels para todos los candidatos; en el flujo eval de 12 trials el **panel 0 ≡ panel 8 pixel-igual** (memorización en reaplicaciones). Fix: celda objetivo + símbolos aleatorizados con `rng` inyectable (rampa conservada — es el diseño); tests (reproducibilidad por seed, sensibilidad, unicidad en 12 trials) + **E2E en smoke: 1/8 panels coincide con la fórmula legacy** (coincidencia al azar en móvil P1; los 7 restantes difieren). Sin estados irresolubles (timeout bound). |
| 5 | Inferencia | ✅ PASS (1 P1 cerrado; 3 P3 abiertos sistemáticos + 1 decisionado) | `skill: 'visual_search_efficiency'` declarado en ambas configs ✓. Chain R-6 completa trazada en `docs/design/modulos/visual_search.md` (**VSP-P1-3, P1 sistemático stable_dg — cerrado; 5.º y último doc**): constructo (búsqueda visual) → demanda (localización de ● entre 7–19 distractores, rampa de set size, timeout 10 s) → conducta (RT de aciertos, errores, timeouts, distancia de localización) → telemetría agregada (`visualSearch.*`) → feature v2 (`game.visualSearchEfficiency`, `game.visualSearchMeanSetSize` — sin reordenar `featureOrder`) → regla provisional (dimensión `visualSearchEfficiency`, `descriptive_only`) → disponibilidad/confianza/caveats → narrativa de revisión humana. Reporte honesto: score provisional **sin baremos** + confidence + caveats; **señal limitada y documentada**: batería de 4 trials = 1 muestra por set size → índice blend (RT+precisión+tamaño), **no** pendiente de búsqueda (P3-9 decisionado; el flujo eval de 12 trials soporta pendiente — siguiente paso de validación). **P3-8 (abierto sistemático):** `talentProfile` sin camino null-score por dimensión — sesión interrumpida antes del juego 4 → `searchEfficiency 0` + `1−errorRate(0)=1` → dimensión "alta" sin evidencia (mismo GNP-P3-8/CIP-P3-11; fix post-B). **P3 (abierto sistemático):** canal EdgeAI `visualSearchEfficiency` = proxy de agregado `taskPerformance` en stable_dg (GNP-P3-7 pattern; fix post-B). **P3-10 (decisionado):** `aria-label` del objetivo ("Objetivo: punto sólido") revela su identidad a lectores de pantalla → demanda distinta del constructo para ese subgrupo; caveat documentado (doc §11.3), no se retira el aria. |
| 6 | Assets | ✅ PASS (1 P3 abierto sistemático) | Cero assets externos (divs + CSS). CSP-safe. Contraste **medido** (sobre blanco del tile): glifo `#334155` **10.35:1** (AAA); feedback correct `#047857` **5.48:1**, incorrect `#b91c1c` **6.47:1**, timeout `#b45309` **5.02:1**, RT-display `#6f503a` **7.70:1** — todos AA ≥4.5:1. Objetivo y distractores comparten contenedor (test: sin revelación por color/preselección). **Glifos unicode (●○◇□△) + 🔎 + ✓✗ renderizan correctamente en headless** (vision-verified en screenshots: formas nítidas, sin tofu — E2E del fix P2-3 también visible en `desktop-vs-panel-1.png`). **P3-6 (abierto sistemático):** real-device pendiente (patrón `t_25009e33`); si tofu → SVG inline. SFX: sin SFX propio en stable_dg (toggle global del stage inerte para este juego — consistente, como B.2/B.3/B.4). |
| 7 | Responsividad | ✅ PASS (1 P2 cerrado + 1 P2 layout cerrado) | **P2-3 (cerrado):** **canvas móvil cortado** — la rejilla se calculaba con `safeWidth = max(260, w)` mientras el task-area se pintaba a `w=240` (piso del stage) → en setSize 20 el borde derecho del tile más externo quedaba en **255 px > 240 px** → recortado por `.task-area { overflow: hidden }` (objetivo potencialmente medio-visible, tap degradado). Fix: rejilla con el tamaño real renderizado (guard solo para NaN/0) + **piso de tile 44 px** (AA touch; antes 42) + CSS `min-width/min-height: 44px`. Test de fit: 240×280 / 312×340 / 520×290 × los 4 set sizes → **todo tile completo dentro del canvas** (caso más justo: margen derecho 2 px, 240×280 setSize 20; gap mínimo entre tiles 4 px — **sin solapamiento**, note (b)). **E2E en smoke: asert de tiles-en-canvas + ≥44 px por panel (4 panels × 2 viewports = 8/8) + vision-verified `mobile-vs20-stimulus`**. **Layout (P2, cerrado):** brief standalone + caption sumaban ~80 px → el juego no cabía sin scroll interno en el stage 290 px desktop → instrucción movida a **header pill** ("Objetivo: punto sólido"), brief/caption retirados (regresión ES+EN testeada; CSS del brief eliminado). **E2E en smoke: `scrollHeight ≤ clientHeight+1` por panel en desktop (fit, patrón B.4 CIP-P2-4)**. Móvil: stage de altura auto (≤520px media) → sin scroll interno; overflow horizontal 0 (smoke). Teclado: 8–20 `<button>` focusables → Tab/Enter juega la tarea completa (fallback de localización al centro del tile, `detail===0`). `prefers-reduced-motion`: sin `@keyframes` propios en el juego → N/A. |
| 8 | Fluidez | ✅ PASS (note (f) revisada) | Sin rAF loop; re-render por panel, no por frame; timeout 10 s + ITI 350 ms (timers ligeros). 1 `getBoundingClientRect()` por respuesta (no por pointermove — **note (f) SRT-P3-2: N/A**, sin pointer sampler en este juego, como B.3/B.4). Sesión larga: resultsRef ≤ 12 panels, sin buffers crudos. Feedback `pointer-events: none` (no interfiere con clicks). **Pitfall de evidencia (B.4):** no se asertó precisión en vivo bajo carga — el timeout E2E (10 s) y los clicks passthrough dejaron holgura amplia; 0 console errors / 0 requestfailed en ambas corridas. |

## Hallazgos

| ID | Sev | Dim | Descripción | Acción |
|----|-----|-----|-------------|--------|
| VSP-P1-1 | P1 (cerrado) | 3/4 | **Sin timeout por trial** — un panel sin click permanecía abierto para siempre (batería no completatable; foco perdido = hang práctico). El resto de la familia stable_dg tiene bound (SimpleRT 3000 ms, GoNoGo 900 ms, CI 3200 ms) | **FIX 2026-09-12 RED→GREEN:** `trialTimeoutMs` (10 000 ms, prop inyectable) → `response{outcome:'timeout', correct:false, timedOut:true}` + feedback "Tiempo agotado" + avance; game_end con score 0 en timeouts. Test unitario + **E2E en smoke** (desktop panel 1 sin click: "Tiempo agotado" visible en DOM+vision, avance a Panel 2) |
| VSP-P1-2 | P1 (cerrado) | 3 | **Geometría dependiente de viewport mid-trial** (rollup B.2→B.5, línea 138 del legado): `useMemo([width, height])` + effect dependiente de la identidad del trial → re-medición del stage (resize/visualViewport) reconstruía trials → estímulo huérfano/duplicado + RT reseteada + tiles reposicionados en curso | **FIX:** `initialSizeRef` (patrón B.2) — trials y task-area bloqueados al montar; test de re-render con size nuevo (sin re-emisión, canvas intacto, 1 response); smoke inamovible |
| VSP-P1-3 | P1 (cerrado) | 5 | **Sin doc módulo R-6** (`docs/design/modulos/visual_search.md`) — P1 sistemático: los 5 stable_dg no tenían doc | **Creada en esta card** (template adaptada; chain completa §0–§17) — 5.º y último de stable_dg → **gap sistemático cerrado** |
| VSP-P2-1 | P2 (cerrado) | 1/5/12 | Summary sin claves de contrato del session builder (`completedTrialCount`, `meanReactionTimeMs`, `meanScore`) → RT/Puntaje de visual_search ausentes de la media de batería y de la card del reporte (mismo bug GNP-P2-1/CIP-P2-1) | **FIX:** keys añadidas a `summarizeVisualSearchResults` (+ test de contrato) + **E2E smoke: card VS en reporte vivo con Puntaje** (75% desktop — con 1 timeout; 100% móvil) |
| VSP-P2-2 | P2 (cerrado) | 2/9 | **Localización degenerada** (note (c)): `pointer` = centro de la celda (`event` sin usar) + `clickDistanceToTargetPx` = distancia celda-celda (0 en acierto, siempre) — la métrica se llamaba "localización" sin medir puntero | **FIX:** coords reales del click relativas al canvas (guard `detail>0` + coords finias), fallback teclado al centro del tile (no se penaliza la localización por no-uso de puntero), distancia euclídea real, null en timeout; tests (mouse `clientX/clientY/detail:1` + teclado) |
| VSP-P2-3 | P2 (cerrado) | 4/7 | **Canvas móvil cortado** (note (b)): rejilla con `safeWidth=max(260,w)` vs task-area pintado a `w=240` → setSize 20: borde derecho del tile en 255 px > 240 → recorte por `overflow:hidden` + **tile 42 px < 44 AA** | **FIX:** rejilla con el tamaño real + piso de tile 44 px (+ CSS min 44) + test de fit 240×280/312×340/520×290 × 4 tamaños + **E2E smoke: tiles completos en canvas + ≥44 px por panel (8/8)** + vision-verified |
| VSP-P2-4 | P2 (cerrado) | 4/5 | **Arrays deterministas por sesión** (`(5i+3)%size`; símbolo `(i+trial)%4`) → mismos panels para todos los candidatos; en eval 12 trials **panel 0 ≡ panel 8** (memorización en reaplicaciones/test-retest) | **FIX:** celda objetivo + símbolos aleatorizados con `rng` inyectable (rampa setSize conservada — diseño); tests (reproducibilidad por seed, sensibilidad al seed, unicidad de panel en 12 trials, 1 objetivo + símbolos permitidos) + **E2E smoke: no-todo-legacy (1/8 coincidía por azar)** |
| VSP-P2-5 | P2 (cerrado) | 3/4/8 | ITI `setTimeout` sin ref/cleanup (onComplete tardío tras abort/unmount) + ritmo fijo 350 ms telegrafiado + **feedback sin estilo en batería** (clase `.trial-feedback` solo bajo `.simple-rt-task` tema oscuro → sin centrado) | **FIX:** `itiRef` + cleanup en unmount/cambio de trial + jitter [0.75,1.25] (test de límites 262.5/437.5 + unmount-during-ITI) + chip `.visual-search-task__feedback` con tone semántico (contrastes AA medidos) + test |
| VSP-P3-1 | P3 (cerrado) | 4 | `durationLabel: '1 min'` vs 6–42 s reales (4 panels) | `'45 s'` en `postulationDemoConfig.js` (+ test; visible en header del smoke) |
| VSP-P3-2 | P3 (cerrado) | 2 | `game_end.gameState.score` 0–1 (resto de la batería: 0–100) | `Math.round(accuracy * 100)` (+ test: score 100 en 1/1) |
| VSP-P3-3 | P3 (cerrado) | 2 | `difficulty` inconsistente (`'set_size'` en game_start / número `setSize` por evento) | unificada a `'visual_search'` en `gameState` (+ test); el setSize vive en el payload estímulo/respuesta |
| VSP-P3-4 | P3 (cerrado) | 3 | StrictMode (dev): `stimulus_shown` duplicado del panel 0 | guard `emittedForRef` por índice + test (1 evento, no 2) |
| VSP-P3-5 | P3 (cerrado) | 3/4 | Sin `handledRef` (doble-tap ultra-rápido → 2 responses + trial duplicado) | `handledRef` por panel (patrón B.3/B.4) + test de doble-click |
| VSP-P3-6 | P3 (abierto, sistemático) | 6 | Glifos unicode (🔎 ●○◇□△ ✓✗) en juego | Headless: OK (screenshots + vision, sin tofu). Real-device pendiente (patrón `t_25009e33`); si tofu → SVG inline |
| VSP-P3-7 | P3 (abierto, sistemático) | 2/5 | `sanitizeGameResults` sin `role: calibration\|scored` (SRT-P2-3, note (g)) | Este juego es `visible:true` (scored) → impacto bajo; fix coordinado con B.1/epic (patrón CIP-P3-9/GNP-P3-10) |
| VSP-P3-8 | P3 (abierto, sistemático) | 5 | `talentProfile` sin camino null-score por dimensión: sesión interrumpida → dimensión "alta" sin evidencia (`1−errorRate(0)=1`) | Mismo GNP-P3-8 (B.3)/CIP-P3-11 (B.4); afecta a todo stable_dg; fix post-B (null-safe por fuente de señal) — doc §11 |
| VSP-P3-9 | P3 (decisionado) | 5 | Batería de 4 trials = 1 muestra por set size → índice blend, no pendiente de búsqueda | Documentado doc §2/§11 (honestidad R-6); flujo eval 12 trials (3/tamaño) soporta pendiente — siguiente paso de validación; NO se cambia el presupuesto de batería (decisión de producto) |
| VSP-P3-10 | P3 (decisionado) | 5 | `aria-label` del objetivo revela su identidad a lectores de pantalla (demanda distinta del constructo) | Caveat doc §11.3 (revisión humana); accesibilidad > validez para ese subgrupo; score `descriptive_only` con confidence |

## Notas de la card — resolución

- **(a)** Doc módulo creada (`docs/design/modulos/visual_search.md`, 5.º/5.º stable_dg — **gap P1 sistemático cerrado**). Constructo verificado: `skill: 'visual_search_efficiency'` en ambas configs; el reporte lo soporta **honestamente**: score provisional sin baremos + caveats + limitación de señal documentada (1 muestra por tamaño → índice blend, P3-9); residual sistémico de sesiones interrumpidas en P3-8.
- **(b)** Set size y proporción target/distractor según diseño: 1 objetivo : N-1 distractores (N = 8/12/16/20, rampa). **Sin solapamiento ni recorte** en canvas 240 px móvil: fix VSP-P2-3 + test de fit (3 tamaños de stage × 4 tamaños de set) + E2E smoke (tiles-en-canvas 8/8) + vision-verified (`mobile-vs20-stimulus`: 5×4 completo).
- **(c)** RT por trial + **localización correcta en telemetría**: fix VSP-P2-2 (coords reales del click, fallback teclado, null en timeout) + 2 tests. La métrica `meanClickDistanceToTargetPx` ahora mide lo que dice.
- **(d)** `game_end`: **YA se emitía** — verificado (línea de completado, antes de `onComplete`); se normalizó escala 0-100 + difficulty unificada (VSP-P3-2/3) + test de regresión (1 evento, tras último response).
- **(e)** i18n de instrucciones: header pill "Objetivo: punto sólido" / "Target: solid dot" + progreso + feedback "Tiempo agotado"/"Time out" — todo `t(es, en)`; brief/caption retirados (layout, dim. 7) con regresión testeada ES+EN.
- **(f)** Pointer sampler caching (SRT-P3-2): **N/A** — visual_search no captura `pointermove` (respuesta discreta en tile, sin pointerSampler), como B.3/B.4. 1 `getBoundingClientRect` por respuesta.
- **(g)** Role calibration/scored (SRT-P2-3): juego `visible:true` = scored → **impacto bajo; P3 abierto sistemático** (VSP-P3-7, tracked con GNP-P3-10/CIP-P3-9).
- **(h)** Ejecutado solo en main, sin otras cards FASE B running (verificado al inicio: única `running` en el board; las B.6–B.12 estaban en `ready`).

## Gates de pre-lanzamiento (checklist del rollup)

- [x] Tests focales GREEN: **119/119** — `visualSearch` (20) + `postulationDemoConfig` (8) + `PostulationGamesDesignSystem` (11) + `gameRerenderStability` (9) + `gameTelemetry` (9) + `PostulationGameStage` + `postulationDemoSessionBuilder` (7) + `batteryRuntime` (4) + `assessmentExperienceSmoke` (2) + `finalAssessmentPayload` (2) + `gameFeatureVector` (5) + `PostulationReportScreen` + `App` (2)
- [x] Regresión completa: **1343/1343 (146 files)** (1330 en B.4 → +13: 12 tests nuevos visualSearch + 1 config)
- [x] oxlint 0 en archivos tocados (`VisualSearchTask.jsx`, `visualSearch.test.jsx`, `postulationDemoConfig.js`, `postulationDemoConfig.test.js`, `PostulationGamesDesignSystem.test.jsx`)
- [x] Build OK (`npm run build`, 5.54 s; warning de chunk size preexistente)
- [x] Smoke 2 viewports (1280×720 mouse + 390×844 touch) en Vite dev + Playwright — `scripts/smoke-b5-visual-search-2026-09-12.mjs`: **0 failures, 0 console errors, 0 requestfailed, 0 overflow**; visual search jugado completo 4/4 panels por viewport (flujo real: precisión → go/nogo → stroop → **búsqueda visual** → reporte en vivo); **E2E del fix P1-1: timeout real del panel 1 desktop ("Tiempo agotado" DOM+vision + avance)**; **E2E del fix P2-4: 1/8 panels no-legacy**; fit del stage (sin scroll interno) por panel en desktop; tiles completos en canvas + ≥44 px por panel (8/8); card VS en reporte vivo con Ensayos/Precisión/**Puntaje**/Tiempo (VSP-P2-1). Complemento visual: `scripts/smoke-b5-visual-search-visuals-2026-09-12.mjs` (estímulos 16/20 tiles + card VS scrolleada). Screenshots: `docs/qa/prelaunch-b5-visual-search/`
- [x] Fixture válido: `?fixture=1` renderiza reporte con card "Búsqueda visual" en ambos viewports (4/90%/87%/740ms — sintético)
- [x] Constructo en reporte: `visualSearchEfficiency` (score provisional, sin baremos, caveats + confidence; limitación de señal 1-muestra-por-tamaño documentada; residual sistémico VSP-P3-8) — doc módulo §11
- [x] Doc módulo `visual_search.md` — creada (VSP-P1-3 cerrado)

## Evidencia

- Fix VSP-P1-1 (timeout): diff en `VisualSearchTask.jsx` (`trialTimeoutMs` + `timeoutRef` + `outcome:'timeout'` + feedback tone timeout) + test unitario (1500 ms inyectado: response timeout, RT 1500, sin pointer, game_end score 0) + E2E en smoke (desktop panel 1: `desktop-vs-timeout.png`).
- Fix VSP-P1-2 (geometría): `initialSizeRef` (patrón B.2) + test (re-render 600×400→300×250: sin re-emisión de stimulus_shown, canvas `width: 600px` intacto, panel invariante).
- Fix VSP-P1-3: `docs/design/modulos/visual_search.md` (42 KB, §0–§17).
- Fix VSP-P2-1 (contrato summary): `summarizeVisualSearchResults` (+`trialCount`, `completedTrialCount`, `meanReactionTimeMs` solo-aciertos, `meanScore`, `timeoutCount`) + test + E2E smoke (card VS con Puntaje en reporte vivo).
- Fix VSP-P2-2 (localización): coords reales del evento (guard `detail>0`) + fallback teclado + tests (mouse: `pointer {x:100,y:120}` + distancia real ≈ hipotenuso; teclado: centro del tile + distancia 0).
- Fix VSP-P2-3 (fit móvil): rejilla con tamaño real + piso 44 px + test (240×280/312×340/520×290 × sizes 8/12/16/20, todo tile dentro del canvas) + E2E smoke (asert de tiles por panel, 8/8) + vision (`mobile-vs20-stimulus.png`, `desktop-vs-panel-1.png`).
- Fix VSP-P2-4 (rng): `buildVisualSearchTrials({rng})` + tests (reproducibilidad lcg(42), sensibilidad lcg(43), unicidad de panel en 12 trials, 1 objetivo + símbolos ⊆ {○◇□△}) + E2E smoke (targetIndex desktop [6,7,1,13] / móvil [3,0,8,0] vs legacy [3,8,13,18] → 1/8 coincidencia por azar).
- Fix VSP-P2-5 (ITI + feedback): `itiRef` + cleanup + jitter (test de límites 262.5/437.5 vía delay registrado + unmount-during-ITI) + CSS `.visual-search-task__feedback` (tones correct/incorrect/timeout; contrastes 5.48/6.47/5.02 AA; vision `desktop-vs-timeout.png`) + test de chip.
- VSP-P3-1..5: `durationLabel: '45 s'` (config + test + visible en header del smoke), `game_end` 0-100 + difficulty `'visual_search'` (test de contrato completo), StrictMode (test), handledRef (test doble-click).
- Privacidad: `trials`/`items` del summary eliminados por `stripForbidden` y re-validados por `validateFinalAssessmentPayload` (recursivo); suite focal incluye `postulationDemoSessionBuilder` (7/7) y `finalAssessmentPayload` (2/2); smoke aserta 0 console errors en el flujo completo.
- Contraste (dim. 6): cálculo WCAG 2.1 — glifo 10.35:1, feedback 5.48/6.47/5.02:1, rt-display 7.70:1 (sobre blanco del tile).
- Smoke: `node scripts/smoke-b5-visual-search-2026-09-12.mjs` → `{failures: [], consoleErrors: [], ok: true}` (2026-09-12, Vite dev 127.0.0.1:5173, chromium-1234 headless, Pi libre tras la suite completa).
