# Auditoría pre-lanzamiento — BalloonRisk (balloon_risk)

**Fecha:** 2026-09-12
**Auditor:** Hermes (FASE B.6, KRU-116)
**Estado:** **✅ PASS — P1×2 + P2×1 cerrados (RED→GREEN + smoke E2E); P3×6 cerrados/decisionados/abiertos sistemáticos**
**Rollup:** `docs/qa/prelaunch-rollup.md` (fila 6)
**Doc módulo R-6:** `docs/design/modulos/balloon_risk.md` (existía — actualizada en esta card: tabla de constructos §1.2 a R-6d, clamp §4, ROUND_SETTLED §7, gates §15)

> **Nota de re-dispatch:** esta card es el 3.er intento (KRU-116). El WIP del run 116 (fixes P1-1/P1-2 + tests + doc + CSS, conservado en tree) se verificó GREEN en este run (34/34 focales + 738/738 regresión amplia). El smoke script del WIP estaba **roto** (testids de laser con guiones `laser-cell-0-0` vs formato real `laser-cell-7,0`, y "skip" imposible: en batería un juego no se salta, se completa) — reescrito y verde en este run.

---

## Contexto

- Batería: `original` (`?battery=original`) — bloque **visible**, **juego 2 de 7**, `skill: 'risk_feedback_adjustment'`, `trialCount: 8`, `durationLabel: '2 min'` (blueprint `originalGameBlueprints.js`; la batería override `visible:true`/`controlled_active` vía `postulationDemoConfig.js`).
- Código: `src/tasks/original-games/BalloonRiskPostulationTask.jsx` (297 líneas). Tests: `.test.jsx` (7→**8 tests**). Tocados además (WIP verificado en este run): `balloonRiskTelemetry.js` (+test), `originalGameFeatureVector.js` (+test), `originalGameThemes.css`, `originalGameAnimations.css`, `docs/design/modulos/balloon_risk.md`. Sin cambios: `balloonRiskFeedback.js` (+test, ya cubría forbidden keys y validación `cashouts+pops ≤ rounds`), `balloonThresholdCalibrationReview.js` (+test, verificado).
- Render en batería: `PostulationGameStage` → `getPostulationGameViewport()` (clamp 240×280..620×340, compartido; medidas de B.5: 520×290 @1280×720, 312×340 @390×844). `getBalloonRiskLayoutMetrics` derivado: compact (w≤620 ∨ h≤360), veryShort (h≤320 → scale cap 1.85), statColumns 2/4.
- Mecánica (BART-like, self-paced): 8 rondas con umbral oculto fijo `[7, 10, 8, 12, 9, 11, 13, 8]` pumps; `pointValue = 10 + (index % 3) * 2` (10/12/14 cíclico); "Inflar" suma puntos en riesgo; "Asegurar" (cashout) banca la ronda; el umbral → pop (pierde la ronda). Agregado `balloon_risk_aggregate_v1` (allowlist de 13 campos escalares).

## Veredicto por dimensión (8)

| # | Dimensión | Veredicto | Evidencia |
|---|-----------|-----------|-----------|
| 1 | Toma de muestras | ✅ PASS (P1 cerrado) | 1 ronda = 1 estímulo + 1 respuesta (8 rondas en batería). Reset por ronda: `roundStartRef` en el effect de estímulo (dedupe `shownRoundsRef` — cubre también StrictMode double-mount: el ref persiste en la remount simulada → sin duplicado). `timeMs` desde `handleIntroDone` (excluye micro-intro). **P1-2 (cerrado):** sin el input lock, la ventana FX (pop 900 ms / cashout 500 ms) permitía una 2.ª respuesta por ronda → doble conteo. Calidad: 8 rondas fijas, umbrales conocidos; práctica (G.2) = micro-intro skippable + `practice` prop (`markPracticeSummary`), sin rondas de calibración en original (bloques `visible:false` no existen — ver note (f)). |
| 2 | Telemetría | ✅ PASS (2 P1 cerrados, 3 P3 decisionados) | Eventos: `game_start` ✓ (GameRuntime, una vez — `startedRef`), `stimulus_shown` ✓ (roundId, targetId, `riskBand`), `response` ✓ (outcome, `reactionTimeMs`, score, bloque `balloonRisk` agregado), **`game_end` ✓** (antes de `onComplete`; `level=8`, `difficulty:'risk_feedback'`, `score=riskEfficiency`). Contrato `game_event_v1` vía `normalizeGameEvent` ✓. Privacidad: agregado allowlist-only (`BALLOON_ALLOWED_RESPONSE_FIELDS` 13 escalares) + `sanitizeBalloonResponsePayload` + forbidden keys (`pumpSequence`, `rawPointerPath`, `pointerSamples`, `clickTrace`, `rawGameEvents`, `trials`) rechazados en feedback y feature vector; **no existe pointer sampler en balloon** (respuesta por botón/teclado, sin trayectoria). Suite focal 34/34 + regresión amplia 738/738 (session builder, finalAssessmentPayload, fixtures). **P3 decisionados:** escala `game_end.score` 0..1 (P3-4); `riskBand` ES en payload estímulo (P3-5); ajuste post-pérdida solo cashout- posterior (P3-6). |
| 3 | Sincronización | ✅ PASS (P1 cerrado) | Máquina: intro → ROUND_LOOP → **ROUND_SETTLED (input locked, B.6)** → avance FX (900/500 ms inyectables `popFxMs`/`cashoutFxMs`) → siguiente ronda ∣ completeGame. Timers trackeados en `timeoutsRef` + cleanup en unmount (línea 57) → sin leak si el stage desmonta mid-FX. `finishRound` solo por acción de usuario (sin effect de auto-avance → el riesgo de stale-outcome del rollup B.5(c) no aplica: no hay effect que observe `levelOutcome`). `settledRoundRef` se setea **sincrónicamente al inicio** de `finishRound` (antes del estado FX) → incluso un doble click en el mismo tick queda bloqueado; `disabled={popFx\|\|cashFx}` cubre el affordance. E2E smoke: 8 rondas mixtas sin respuesta duplicada, avance a juego 3 verificado. |
| 4 | Gameplay | ✅ PASS (1 P3 decisionado) | Reglas según spec (doc §4): umbral oculto (no se revela antes del pop — test W2 "tension from 2nd pump without revealing thresholds"), cashout siempre viable → **sin estados irresolubles/hang** (toda ronda termina en pop o cashout). Edge: doble-input (P1-2 cerrado), foco perdido = espera self-paced (sin timeout por ronda — P3-3 decisionado, patrón BART), abandono = cleanup de timers. Micro-intro 3 slides ES/EN skippable (sin telemetría — test W3). Dificultad: umbrales fijos por ronda (no aleatorios) — reproducible; variación = bandas alto/medio/bajo (7-8 alto, 9-10 medio, 11-13 bajo). |
| 5 | Inferencia | ✅ PASS (doc R-6 actualizada; 1 P3 abierto sistemático) | Chain R-6 **completa y verificada en código** (doc módulo §1.2/§11/§13 actualizada a R-6d en WIP): constructo `riskFeedbackProfile` → demanda (decisión riesgo/recompensa bajo umbral oculto, 8 rondas) → conducta (pumps, cashout/pop, ajuste post-pérdida) → telemetría agregada (`balloon_risk_aggregate_v1`) → feature `original_game_feature_vector_v1` (`balloon.*` índices 6–13, `featureOrder` sin reordenar) → regla provisional (`buildRiskFeedback`: `0.35·riskEff + 0.25·cashoutRate + 0.25·(1−popRate) + 0.15·balancedExploration`, `provisional_score` conf ≤0.55, caveats `frustration_tolerance_not_measured` + `risk_index_not_personality_trait` + `game_strategy_score_not_normative_trait`) → disponibilidad (`measured_complete/partial` → `insufficient` si falta señal — test "requires complete…") → narrativa (reporte: availabilityLabel "Lectura preliminar" + demoExplanation + caveats). `decisionMaking` = `descriptive_only` conf ≤0.2 (balloon solo evidencia parcial; test "does not convert higher risk-taking into a better talent score" ✓). Ajuste post-pérdida = **máscara de evidencia** (`postLossAdjustmentObserved`), sin constructo propio; `not_observed` → desconocido, nunca "bajo". Leadership/communication = `not_measured`. **Nota R-6 base vs R-6d:** la card pide "riesgo/decisión = descriptive_only" (R-6 original); el estado vigente R-6d (AGENTS.md §estado: "8 constructos con señal provisional, reporte sin `No medido`") otorga `provisional_score` a `riskFeedbackProfile` (conf ≤0.55 + 3 caveats) y mantiene `decisionMaking` `descriptive_only` (≤0.2) — la invariant crítica (jamás tolerancia a la frustración) se cumple en las 4 capas: mapping (caveats+narrative, testeado), feedback (`reviewerCaveat`), reporte (demoExplanation — verificado en smoke fixture), doc módulo. **P3-2 (abierto sistemático):** `sanitizeGameResults` sin `role: calibration|scored` (SRT-P2-3) — N/A para balloon (batería original sin bloques warmup). |
| 6 | Assets | ✅ PASS (1 P3 abierto sistemático) | Cero assets externos: CSS + emoji + **SFX WebAudio** (osciladores, sin archivos, sin red; default OFF; toggle global del stage, 44 px tras P2-1). CSP-safe. Smoke: 0 `requestfailed` en ambos viewports. Emoji 🎈💥🔒 renderizan en headless (pop FX vision-verified en `desktop-balloon-pop.png`) → **P3-1 (abierto sistemático):** real-device pendiente (patrón SRT-P3-1/VSP-P3-6); si tofu → SVG inline. |
| 7 | Responsividad | ✅ PASS (P2 cerrado) | Touch targets: botones `.primary`/`.secondary` **min-height 48 px** (postulationDemo.css, ≥44 AA) + `focus-visible` (primary compartido; secondary balloon-specific añadido en WIP — el gradiente "Cielo" redefinía la variante) + `disabled` (WIP: opacidad 0.5, cursor not-allowed, sin sombra). HUD 2 cols en compact / 4 en desktop. Teclado: `↑/Espacio/+` inflar, `↓/s/S/-` asegurar (hint visible; `balloonKeyAction`). `prefers-reduced-motion`: bob/shake/burst envueltos en `@media (no-preference)` (WCAG 2.3.3 ✓). **E2E smoke: 0 overflow horizontal en 1280×720 y 390×844** (assert en laser, balloon y reporte por viewport) + screenshots (10) en `docs/qa/prelaunch/balloon-risk-2026-09-12/`. |
| 8 | Fluidez | ✅ PASS | Sin rAF loop (decision-driven; re-render por pump, no por frame). SFX no bloquea hilo principal (WebAudio scheduling en gesture; default OFF). `reactionTimeMs` por respuesta (input latency registrada en el evento). Sin `getBoundingClientRect` en render path (note (f): N/A — sin pointer sampler). Sesión larga: refs ≤ 8 entradas (`pumpCountsRef`), sin buffers crudos; GC trivial. Smoke E2E: 8 rondas + 3 niveles laser en vivo, 0 console errors (ambos viewports). |

## Hallazgos

| ID | Sev | Dim | Descripción | Acción |
|----|-----|-----|-------------|--------|
| BLN-P1-1 | P1 (cerrado) | 1/5 | **`riskEfficiency` excedía [0,1] en juego óptimo** — máximo alcanzable 8 rondas = Σ(threshold−1)·pointValue = 60+108+98+110+96+140+120+84 = **816 > 800** (denominador nominal `total·100`) → un jugador óptimo (0 pops) emitía 1.02 y **invalidaba el bloque aguas abajo**: `validRatio` del feature vector → `invalid_aggregate`; `ratio()` de feedback → `not_available`; mapping → `insufficient` (el mejor jugador perdía toda la señal) | **FIX (WIP run 116, verificado RED→GREEN en este run):** clamp `Math.min(1, Math.max(0, raw))` en la fuente (`buildBalloonResponseAggregate`) + clamp defensivo + quality flag `balloon_risk_riskEfficiency_clamped` en `addBalloonFeatures` (patrón passenger, pitfall #71) + 2 tests (telemetry: 816/0 pops → ≤1; feature vector: 1.02 → 1 + flag + bloque `measured_complete`) + doc §4/§11. Normalización por máximo real (v2) pendiente de calibración con normas (documentado). |
| BLN-P1-2 | P1 (cerrado) | 1/3 | **Doble respuesta en la ventana FX** — entre `finishRound` y `advanceToNextRound` (pop 900 ms / cashout 500 ms) click y teclado seguían activos → 2.ª respuesta por ronda, `popCount`/`cashoutCount` doble-contado, agregado corrupto (`cashouts+pops > roundsCompleted` → feature vector + feedback inválidos) | **FIX (WIP, verificado):** `settledRoundRef` (seteado síncrono al inicio de `finishRound`, cleared en `advanceToNextRound`) + guard en `pump()`/`cashout()` + botones `disabled={popFx\|\|cashFx}` + CSS `:disabled`/`:focus-visible` de `.secondary` + test E2E de componente (7 pumps → pop; keyDown ` ` + click durante FX → exactly 1 `response` por ronda; final `popCount: 1, cashoutCount: 1`). |
| BLN-P2-1 | P2 (cerrado) | 7 | **SFX toggle 42 px < 44 px** (AA touch, dim. 7) en `originalGameAnimations.css` | **FIX (WIP):** 42→44 px. Verificado en smoke (toggle visible en screenshots; sin overflow). |
| BLN-P3-1 | P3 (abierto, sistemático) | 6 | Emoji 🎈💥🔒 en juego/micro-intro | Headless OK (vision-verified, sin tofu). Real-device pendiente (patrón `t_25009e33`/SRT-P3-1/VSP-P3-6); si tofu → SVG inline. |
| BLN-P3-2 | P3 (abierto, sistemático) | 5 | `sanitizeGameResults` sin `role: calibration\|scored` (SRT-P2-3) | Batería original sin bloques `visible:false` → balloon siempre scored (impacto N/A); fix coordinado post-B (patrón VSP-P3-7/GNP-P3-10/CIP-P3-9). |
| BLN-P3-3 | P3 (decisionado) | 4 | Sin timeout por ronda (self-paced) | Diseño BART: toda ronda termina en pop/cashout → sin hang, sin pérdida de datos; un jugador que se detiene extiende la sesión (aceptable para tarea de decisión; documentado doc §2/§7). |
| BLN-P3-4 | P3 (decisionado) | 2 | `game_end.gameState.score` = `riskEfficiency` 0..1 vs 0..100 en stable_dg (VSP-P3-2) | Escala por-agregado (el score del evento es metadado diagnóstico; la feature usa el agregado directamente, no `gameState.score`). Decisión: mantener por juego; normalizar escala de eventos en v2 si se agregan métricas cross-game sobre events. |
| BLN-P3-5 | P3 (decisionado) | 2 | `riskBand: 'alto'\|'medio'\|'bajo'` (ES) en payload de `stimulus_shown` — resto de payloads original numéricos-only (laser) / EN labels (review module `high/medium/low`) | Data-only, sin consumo aguas abajo (verificado por grep). Decisión: mantener (esquema estable `balloon_risk_round`); normalizar en v2 del agregado. |
| BLN-P3-6 | P3 (decisionado) | 5 | Ajuste post-pérdida solo se registra si la ronda siguiente termina en **cashout** (pop→pop sobreescribe `previousPopPumpRef` sin registrar) | Definición documentada (doc §11: `postPopAdjustment` solo si `postPopAdjustmentCount > 0`); la máscara `postLossAdjustmentObserved` protege la interpretación (0 ≠ ajuste nulo). Decisión: mantener; en v2 considerar registro pop→pop como "no ajuste observable". |

## Notas de la card — resolución

- **(a)** Doc módulo **existía** (no P1 como en stable_dg) — verificada y **actualizada** (WIP): tabla de constructos §1.2 a R-6d (provisional_score ≤0.55 + caveats; decisionMaking descriptive ≤0.2; post-loss máscara; leadership/communication not_measured), clamp §4, `ROUND_SETTLED` §7, gates §15. Chain R-6 **completa** tras los cambios (feature `balloon.*` intacta, `featureOrder` sin reordenar).
- **(b)** Constraint R-6 crítico: **el reporte NUNCA claima tolerancia a la frustración** — verificado en 4 capas: (1) mapping caveats `frustration_tolerance_not_measured` + narrative "no mide personalidad ni tolerancia a la frustración" (testeado en `originalGameTalentMapping.test.js`); (2) `balloonRiskFeedback.reviewerCaveat` ("…no son fracaso personal, impulsividad clínica ni tolerancia a la frustración" / "Do not interpret… as frustration tolerance"); (3) demoExplanation del reporte (smoke fixture: "tolerancia a la frustración" presente **solo en framing negativo**); (4) doc módulo §1.1/§13. Riesgo/decisión: `decisionMaking` = `descriptive_only` (≤0.2, sin dirección normativa); `riskFeedbackProfile` = `provisional_score` (≤0.55) conforme R-6d vigente (AGENTS.md) — la wording "descriptive_only" de la card es el R-6 base; el estado R-6d es más específico y está testeado.
- **(c)** Threshold calibration review (`balloonThresholdCalibrationReview.js`, 152 líneas + test): umbrales y cash-out **documentados** (doc §4: 816 máx real, denominador 800, penalización `1−min(0.6, pops·0.12)`); bandas `high/medium/low` (3/2/3 en 8 rondas → `authoringValid`, `valid_for_internal_demo`); **métricas = agregados** (`pumps`, `cashouts`, `pérdidas`, rates) **sin secuencia cruda** (`privacy: authoringOnly: true, rawThresholdsExported: false, candidatePumpSequenceRequired: false`).
- **(d)** SFX: WebAudio oscillators (sin archivos/red), default OFF, trigger en user gesture, `play()` try/catch no-op — **no bloquea hilo principal** (dim. 8 ✓).
- **(e)** Responsive: no hay `<canvas>` (arena DOM/CSS); `getBalloonRiskLayoutMetrics` por viewport (compact/veryShort/scale caps/cols HUD); **0 overflow en 390×844 y 1280×720** (smoke, asserts en 3 superficies por viewport).
- **(f)** Role calibration/scored (SRT-P2-3): balloon = `visible:true` en batería original → **scored** (sin warmup en original). El flag aguas abajo no existe aún → P3 sistemático (BLN-P3-2), impacte N/A aquí.
- **(g)** Ejecución serial: sin otra card FASE B en `running` al inicio (B.7–B.11 en `ready`); único worker activo (cola FASE B secuencial).

## Gates de pre-lanzamiento (checklist del rollup)

- [x] Tests focales GREEN: **34/34** (5 files: BalloonRiskPostulationTask 8 + balloonRiskTelemetry + balloonRiskFeedback + balloonThresholdCalibrationReview + originalGameFeatureVector)
- [x] Regresión amplia GREEN: **738/738 (79 files)** — `src/assessment` + `src/postulation-demo` + `src/tasks/original-games` + `src/validation` (cubre session builder, finalAssessmentPayload, fixtures, design system, talent mapping)
- [x] Suite completa: **1346/1346 (146 files)** (1343 en B.5 → +3: 2 tests balloon WIP + 1 feature vector)
- [x] oxlint 0 en archivos tocados (oxlint 1.81.0: componente + tests + telemetry + feature vector + CSS + smoke script)
- [x] Build OK (`npm run build`, 6.12 s; warning de chunk size **preexistente** — hallazgo G.1, no introducido por B.6)
- [x] Smoke 2 viewports (1280×720 + 390×844) en Vite dev + Playwright — `scripts/smoke-b6-balloon-risk-2026-09-12.mjs`: **PASSED** (0 failures, 0 console errors, 0 requestfailed, 0 overflow). Parte A (vivo): consent → laser 3/3 niveles resueltos (soluciones embebidas) → **balloon 8/8 rondas (4 pops con 💥 verificado + 4 cashouts)** → avance a juego 3 (passenger) en el mismo batch (sin finished screen — patrón B.5(a)). Parte B (fixture `?fixture=1&battery=original`): reporte completo 7/7 con card Balloon (Rondas 8/8, Eficiencia riesgo 72%, Asegurados 6, Tiempo 68 s, feedback "Estrategia riesgo/recompensa" + caveat R-6), constructo "Asunción de riesgo y feedback" "Lectura preliminar", "Toma de decisiones (descriptiva)" "Lectura descriptiva", tags R-6d ("Score provisional" ×10, "Sin baremos · no comparable"), framing negativo "tolerancia a la frustración" presente, **cero "No medido"**. Screenshots: `docs/qa/prelaunch/balloon-risk-2026-09-12/` (10).
- [x] Fixture válido: `?fixture=1&battery=original` renderiza el reporte con card Balloon en ambos viewports (sintético, banner de fixture visible — vision-verified)
- [x] Constructo en reporte: chain R-6 completa (note (b)); `riskFeedbackProfile` provisional + caveats; `decisionMaking` descriptiva; post-loss máscara; `score: null` donde no hay señal (mapping insuficiente testeado)
- [x] Doc módulo `balloon_risk.md` actualizada (notas (a); checkboxes §15 completados)

## Evidencia

- Fix BLN-P1-1 (clamp): diff en `balloonRiskTelemetry.js` (`Math.min(1, Math.max(0, rawEfficiency))`) + `originalGameFeatureVector.js` (clamp defensivo + `balloon_risk_riskEfficiency_clamped`) + 2 tests (816 pts → ≤1; 1.02 histórico → 1 + flag + `measured_complete`) + doc §4/§11.
- Fix BLN-P1-2 (input lock): diff en `BalloonRiskPostulationTask.jsx` (`settledRoundRef` + guards + `disabled`) + `originalGameThemes.css` (`.secondary:disabled`/`:focus-visible`) + test de componente (1 `response` por ronda bajo keyDown+click concurrentes en FX; `popCount:1, cashoutCount:1` final).
- Fix BLN-P2-1: `originalGameAnimations.css` 42→44 px (SFX toggle).
- Smoke reescrito: `scripts/smoke-b6-balloon-risk-2026-09-12.mjs` (el WIP tenía testids de laser incorrectos `laser-cell-0-0`/`-0-4` vs formato real `laser-cell-<x>,<y>` y un "skip" imposible en batería; ahora completa laser 3/3 niveles con `LASER_SOLUTIONS` embebidas, juega balloon mixto, aserta avance a passenger y verifica el reporte fixture con 14 asserts case-insensitive — `text-transform: uppercase` del reporte altera `innerText`, lección de esta corrida).
- Vision-verified: `desktop-balloon-pop.png` (Ronda 5 de 8, HUD 9/0/2/3 = pops r0,r2,r4 + cashouts r1,r3, pips por resultado, header dots 1=teal/laser done, 2=tan/balloon current — confirmado en crop; emojis sin tofu; sin clipping) y `desktop-fixture-report.png` (hero "Reporte de muestra listo para revisión humana", 7/7 juegos, "Integridad de archivos verificada · no implica validez psicométrica", banners fixture + batería original, executive summary; sin overflow).
- Regresión: suite completa (arriba) — sin roturas cross-game por el cambio compartido en `originalGameFeatureVector.js`.
- Pi libre durante el smoke (sin suite vitest en paralelo — patrón B.4).
