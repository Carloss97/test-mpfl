# Auditoría pre-lanzamiento — GoNoGo (go_nogo)

**Fecha:** 2026-09-12
**Auditor:** Hermes (FASE B.3, KRU-116)
**Estado:** **✅ PASS — P1×2 y P2×4 cerrados en esta card (RED→GREEN + smoke E2E); P3×7 cerrados/decisionados**
**Rollup:** `docs/qa/prelaunch-rollup.md` (fila 3)
**Doc módulo R-6:** `docs/design/modulos/go_nogo.md` (creada en esta card)

---

## Contexto

- Batería: `stable_dg` — bloque **visible** (juego 2 de 4 en "Juego X de N"), `skill: 'inhibitory_control'`, `trialCount: 8`, `durationLabel: '1 min'` → **fix a `'15 s'`** (GNP-P3-1: 8 trials × ~1.25 s ≈ 10–15 s reales). En `batteryConfig.js` (assessment) también existe con `trialCount: 24` (standardized) / 8 (demo).
- Código: `src/tasks/GoNoGoTask.jsx` (288→393 líneas). Tests: `src/tasks/goNoGo.test.jsx` (7→15 tests). Telemetría shared: `gameTelemetry.js` (+1 test de agregación post-error), `gameFeatureVector.js`.
- Render en batería: `PostulationGameStage` pasa `width/height` desde `getPostulationGameViewport()` (medido 520×290 @1280×720 y 312×340 @390×844 — mismo stage que B.2) + `trialCount` del bloque. **`height` era ignorada (300 px fijo) → fix GNP-P2-4.**
- Mecánica: semáforo de impulso — pulso en GO, withhold (timeout) en NO-GO; comisión/omisión; post-error slowing.

## Veredicto por dimensión (8)

| # | Dimensión | Veredicto | Evidencia |
|---|-----------|-----------|-----------|
| 1 | Toma de muestras | ✅ PASS (1 P2 cerrado) | 1 respuesta por trial (click discreto, sin pointer sampler — note (d) N/A). Clock único `performance.now()` (pitfall 23 ✓). `stimulus_shown` por trial + reset por trial (`handledRef`). **P2-1 (cerrado):** el summary usaba `totalTrials`/`correctGoRT` — sin las claves de contrato del session builder (`completedTrialCount`, `meanReactionTimeMs`) → la RT de go_nogo no entraba a la media de batería. Fix: claves añadidas (`meanReactionTimeMs = correctGoRT`, documentado: RT medible solo en trials GO) + `goTrialCount`/`noGoTrialCount`. Calidad: piso de 2 NO-GO garantiza denominador de comisión ≥ 2 (GNP-P1-1). |
| 2 | Telemetría | ✅ PASS (2 P2 + 2 P3 cerrados) | Eventos: `game_start` ✓ (GameRuntime), `stimulus_shown` ✓ (cue + responseRequired), `response` ✓ (outcome, RT, bloque `inhibition`, **postErrorSlowingMs por respuesta — fix GNP-P2-2**), **`game_end` ✓ — verificado (note (b)): ya se emitía** (rama de completado, antes de `onComplete`). Contrato `game_event_v1` vía `normalizeGameEvent` ✓. Privacidad: `trials` del summary **se elimina** en `stripForbidden` (`ASSESSMENT_FORBIDDEN_KEYS` incluye `trials`) + re-validación recursiva en `validateFinalAssessmentPayload` ✓ (test session builder: `result` sin `trials`, `payload.validation.ok`). **P2-2 (cerrado):** `postErrorSlowingMs` solo existía en el resumen del juego → el agregado por eventos (`summarizeGameEvents` → feature vector `response.postErrorSlowingMs`) quedaba siempre 0. Fix: emisión incremental por respuesta (misma semántica que el resumen: RT finito tras error, delta vs media de correct_go previos) + test en `gameTelemetry.test.js`. **P3-2 (cerrado):** `game_end.gameState.score` 0–1 vs 0–100 en el resto → `Math.round(meanScore*100)`. **P3-3 (cerrado):** `difficulty` inconsistente (`response_inhibition` en game_start vs `go_no_go`) → unificada. Nota sistémica (documentada, sin fix): el effect del hijo corre antes que el del padre → `stimulus_shown` precede a `game_start` en TODOS los juegos GameRuntime; los tests no asumen orden. |
| 3 | Sincronización | ✅ PASS (1 P3 cerrado) | Máquina de trial: SO 900 ms → pulso/timeout → ITI → trial+1. Todo trial termina (pulso o timeout) → **sin estados irresolubles/hang**. `handledRef` = 1 finalización por trial (double-click/multitouch no-op). Timers cleanup en change/unmount ✓. ITI aleatorio **[0.75, 1.25] × itiMs** (fix GNP-P2-5; antes fijo 350 ms — el ritmo telegrafiaba el timing). Orden de finalización = orden de resultados (secuencial) → post-error en orden de finalización (regla skill #12 ✓). **P3-6 (cerrado):** StrictMode (dev) duplicaba `stimulus_shown` del trial 0 (effect doble, refs persisten) → guard `emittedForRef` por índice + test. Inmune al bug de reset por re-medición de viewport (B.2/B.5): la geometría no puntúa (no hay `useMemo` de trials sobre width/height). |
| 4 | Gameplay | ✅ PASS (1 P1 + 1 P2 + 1 P3 cerrados; 2 P3 decisionados) | **P1 (GNP-P1-1, cerrado):** secuencia **determinista** `noGoEvery: 2` (hardcodeada) → GO/NO-GO/GO/NO-GO…: tras 2 trials el candidato predecía el tipo de cada señal y 100% de precisión era alcanzable **sin inhibición** — constructo inválido (note (c) de la card). Fix: `buildGoNoGoTrials` aleatorizado (p(NO-GO)=0.35, trial 0 = GO para enseñar el mapeo, piso `minNoGo=2`, tope `maxNoGo=count/2`, `rng` inyectable) + tests de propiedades + **smoke E2E: secuencias en vivo no alternantes en 2 viewports**. **P2-5 (cerrado):** ITI fijo → jitter (dim. 3). **P3-1 (cerrado):** `durationLabel '1 min'` → `'15 s'` (8 trials ≈ 10–15 s). **P3-11 (decisionado):** sin feedback por trial — por diseño (feedback inmediato sesga el ajuste medido: el post-error slowing es la señal de regulación; resumen final sí existe). **P3-12 (decisionado):** SO fijo 900 ms — duración de presentación estándar en go/no-go web. Withhold solo por timeout (skill #11 ✓): pulsar en NO-GO = `commission_error` siempre. Práctica completatable: flujo real jugado 8/8 trials en smoke (nota: stable_dg no tiene botón de práctica G.2 — micro-bloques + 8 trials cortos; verificado en vivo). |
| 5 | Inferencia | ✅ PASS (1 P1 cerrado; 2 P3 abiertos sistemáticos documentados) | `skill: 'inhibitory_control'` declarado en ambas configs ✓ — **go/no-go = inhibición/control, NO velocidad pura** (note (a) verificado; la velocidad de GO se agrega a la media de batería, el claim propio es inhibición). Chain R-6 completa trazada en `docs/design/modulos/go_nogo.md` (GNP-P1-2, P1 sistemático stable_dg — cerrado): demanda (GO/NO-GO aleatorizado) → conducta (comisión/omisión/RT/post-error) → telemetría agregada (`inhibition.*`) → feature v2 (`response.*`) → dimensión `inhibitoryControl` (talentProfile) → reporte (metricas por defecto Ensayos/Precisión/Puntaje/Tiempo leyendo `correctGoRT`; score provisional sin baremos, `descriptive_only`). Reporte honesto: **score nunca "alto" sin señal de inhibición** (piso 2 NO-GO → denominador garantizado, §2). **P3-7 (abierto sistemático):** canal EdgeAI `inhibitoryControl` = proxy de agregado (mismo patrón B.2 PRE-P2-3) → mapeo R-6 post-B. **P3-8 (abierto sistemático):** `talentProfile` sin camino null-score por dimensión (sesión interrumpida → comisión/omisión 0 → `1−0` se leería alto); afecta a todo stable_dg; fix post-B. **P3-10 (abierto sistemático, note (e)):** `role calibration/scored` en `sanitizeGameResults` (SRT-P2-3) — este juego es `visible:true` (scored) → impacto bajo; tracked en rollup/epic. |
| 6 | Assets | ✅ PASS (1 P3 abierto sistemático) | Cero assets externos (divs + CSS). CSP-safe. **P3-5 (abierto):** emoji 🚦 en título — renderiza correcto en headless Chromium (verificado en screenshots de smoke: no es tofu); verificación real-device pendiente (patrón `t_25009e33`); si tofu → SVG inline. SFX: sin SFX propio en stable_dg (toggle global del stage inerte para este juego — consistente). |
| 7 | Responsividad | ✅ PASS (1 P2 cerrado) | **P2-4 (cerrado):** `height` prop ignorada (300 px fijo) → en desktop-compact el stage mide 290 px → scroll interno de 10 px; fix: `height` prop (default 300). Smoke 2 viewports: **0 overflow horizontal** en 1280×720 y 390×844 (page.evaluate en setup, stage, por señal GO/NO-GO y transición). Botón "Responder ahora" `min-height: 48 px` ≥ 44 AA (asert smoke + CSS). Task-area 520×290 desktop / 312×340 móvil; cue card `clamp(3rem, 9vw, 4.8rem)` + `nowrap` — "NO-GO" (5 chars) cabe en 312 px (vision-verified sin clip). Teclado: botón `<button>` → Enter/Space juega la tarea completa ✓. `prefers-reduced-motion`: sin animaciones propias → N/A. |
| 8 | Fluidez | ✅ PASS (note (d) revisada) | Sin rAF loop (timeout-driven, SO 900 ms + ITI jitter ≤ 438 ms); re-render por trial, no por frame. **Note (d) SRT-P3-2 (pointer sampler caching): N/A** — go/nogo no captura `pointermove` (respuesta discreta en botón; sin pointerSampler). ITI timers no bloquean hilo principal. SFX N/A (stable_dg). Sesión larga: sin buffers acumulativos (resultsRef ≤ 24 trials; sin samples crudos). |

## Hallazgos

| ID | Sev | Dim | Descripción | Acción |
|----|-----|-----|-------------|--------|
| GNP-P1-1 | P1 (cerrado) | 4/5 | **Secuencia de cues determinista** (`noGoEvery: 2` hardcodeada → GO/NO-GO alternante): el tipo de cada trial era predecible desde el 3° — 100% de precisión sin inhibición; el constructo `inhibitory_control` se medía inválido | **FIX 2026-09-12 RED→GREEN:** `buildGoNoGoTrials({count, noGoProbability=0.35, minNoGo=2, maxNoGo=count/2, rng=Math.random})` — trial 0 GO (enseña el mapeo), piso/tope de NO-GO, rng inyectable (Fisher-Yates con clamp). 5 tests nuevos (propiedades, piso, tope, counts pequeños, reproducibilidad) fallando antes del fix. **Smoke E2E:** secuencias en vivo desktop `GO,NO,NO,GO,GO,NO,NO,GO` (4 NO) y mobile `GO,GO,NO,NO,NO,GO,GO,GO` (3 NO) — ambas dentro de [2,4] y no alternantes |
| GNP-P1-2 | P1 (cerrado) | 5 | Sin doc módulo R-6 (`docs/design/modulos/go_nogo.md`) — P1 sistemático: los 5 stable_dg no tenían doc | **Creada en esta card** (template `plantilla-modulo-original-game.md` adaptada; chain completa §0–§17) |
| GNP-P2-1 | P2 (cerrado) | 1/12 | Summary sin claves de contrato del session builder (`completedTrialCount`, `meanReactionTimeMs`) → la RT de go_nogo no entraba a `summarizeCompletedBlocks.meanReactionTimeMs` de la batería (el key esperado `meanRT` tampoco existía; `correctGoRT` no era leído ahí) | **FIX:** `meanReactionTimeMs = correctGoRT` (documentado: RT medible solo en trials GO) + `completedTrialCount` + `goTrialCount`/`noGoTrialCount`; test aserta el contrato |
| GNP-P2-2 | P2 (cerrado) | 2/5/11 | `postErrorSlowingMs` solo en el resumen del juego: el agregado por eventos (`summarizeGameEvents.inhibition.postErrorSlowingMs` → feature vector `response.postErrorSlowingMs`) siempre 0 → la feature decía "sin ajuste post-error" con datos reales disponibles | **FIX:** `computePostErrorSlowingMs` incremental en `finalizeTrial` (misma semántica que el resumen: RT finito tras error, delta vs media de correct_go previos) emitido en el evento `response` + test de agregación en `gameTelemetry.test.js` (3 respuestas → `inhibition.postErrorSlowingMs: 40`) |
| GNP-P2-4 | P2 (cerrado) | 7 | `height` prop ignorada (300 px fijo) vs stage 290 px en desktop-compact → scroll interno | **FIX:** `height` prop (default 300) + test (312×340 → 340 px) |
| GNP-P2-5 | P2 (cerrado) | 3/4 | ITI fijo 350 ms → ritmo telegrafiado (note (c): "ITI aleatorio, sin telegrafíedar") | **FIX:** jitter [0.75, 1.25] × itiMs + test de límites (a 74 ms del response no avanza; a 126 ms sí) |
| GNP-P3-1 | P3 (cerrado) | 4 | `durationLabel: '1 min'` vs ~10–15 s reales | Fix: `'15 s'` en `postulationDemoConfig.js` (visible en header de smoke) |
| GNP-P3-2 | P3 (cerrado) | 2 | `game_end.gameState.score` 0–1 (resto: 0–100) | Fix: `Math.round(meanScore*100)` + test |
| GNP-P3-3 | P3 (cerrado) | 2 | `difficulty` inconsistente (`response_inhibition` vs `go_no_go`) | Fix: unificada a `go_no_go` + test |
| GNP-P3-4 | P3 (cerrado) | 4 | Campo `noWrap` muerto (retornado, nunca usado) | Fix: eliminado + test |
| GNP-P3-5 | P3 (abierto, sistemático) | 6 | Emoji 🚦 en título (+ SFX toggle 🔊/🔇 shared) | Headless: OK (screenshots). Real-device pendiente (patrón `t_25009e33`); si tofu → SVG inline |
| GNP-P3-6 | P3 (cerrado) | 3 | StrictMode (dev): `stimulus_shown` duplicado del trial 0 (effect doble) | Fix: guard `emittedForRef` por índice + test (1 event, no 2) |
| GNP-P3-7 | P3 (abierto, sistemático) | 5 | Canal EdgeAI `inhibitoryControl` = proxy de agregado (evidencia `candidate_game_stage`, no específico del juego) | Mismo patrón que B.2 PRE-P2-3; fix en mapeo R-6 post-B (documentado en doc §11) |
| GNP-P3-8 | P3 (abierto, sistemático) | 5 | `talentProfile` sin camino null-score por dimensión: sesión sin datos de go_nogo → comisión/omisión 0 → `1−0` se leería "alto" (R-6: señal ausente = desconocida) | Afecta a todo stable_dg; fix post-B (null-safe por fuente de señal); mitigado en este juego por el piso de 2 NO-GO (doc §11) |
| GNP-P3-10 | P3 (abierto, sistemático) | 2 | `sanitizeGameResults` sin `role: calibration\|scored` (SRT-P2-3, note (e)) | Este juego es `visible:true` (scored) → impacto bajo; fix coordinado con B.1/epic; tracked en rollup |
| GNP-P3-11 | P3 (decisionado) | 4 | Sin feedback por trial | Por diseño (sesga post-error slowing); documentado doc §14 |
| GNP-P3-12 | P3 (decisionado) | 3 | SO fijo 900 ms | Estándar go/no-go web; documentado doc §14 |

## Notas de la card — resolución

- **(a)** Doc módulo creada (`docs/design/modulos/go_nogo.md`); constructo verificado: `skill: 'inhibitory_control'` en ambas configs (go/no-go = inhibición, NO velocidad pura); reporte lo soporta honestamente: dimensión con score provisional + caveats, y **señal de inhibición garantizada** (piso 2 NO-GO → commission rate siempre medible; el caso "score alto sin señal" solo queda en sesiones interrumpidas — GNP-P3-8 sistémico).
- **(b)** `game_end` **YA se emitía** — verificado (grep + test de regresión 1 evento tras último response, escala 0-100 post-fix).
- **(c)** Balance de probabilidades + timing: **fix GNP-P1-1** (p=0.35, piso 2, tope ½, trial 0 GO) + **fix GNP-P2-5** (ITI jitter [0.75, 1.25]×350 ms). Smoke E2E con secuencias no alternantes en vivo.
- **(d)** Pointer sampler caching (SRT-P3-2): **N/A** — go/nogo no usa pointerSampler (respuesta discreta en botón, sin `pointermove`).
- **(e)** Role calibration/scored (SRT-P2-3): juego `visible:true` = scored → **impacto bajo; P2/P3 abierto sistemático** (tracked en rollup, fix con epic/B.1).
- **(f)** Ejecutado solo en main, sin otras cards FASE B running (verificado al inicio: única `running`).

## Gates de pre-lanzamiento (checklist del rollup)

- [x] Tests focales GREEN: `goNoGo` (15) + `gameTelemetry` (7) + `gameRerenderStability` + `postulationDemoConfig` (6) + `postulationDemoSessionBuilder` (7) + `PostulationGameStage` + `PostulationQaFixes` + `batteryRuntime` (4) + `assessmentExperienceSmoke` (2) + `finalAssessmentPayload` (2) + `gameFeatureVector` (5) = **88/88 (11 files)**
- [x] oxlint 0 en archivos tocados (`GoNoGoTask.jsx`, `goNoGo.test.jsx`, `gameTelemetry.test.js`, `postulationDemoConfig.js`)
- [x] Build OK (`npm run build`, 5.9 s; warning de chunk size preexistente)
- [x] Smoke 2 viewports (1280×720 mouse + 390×844 touch) en Vite dev + Playwright — `scripts/smoke-b3-go-nogo-2026-09-12.mjs`: **0 failures, 0 console errors, 0 requestfailed, 0 overflow**; go/nogo jugado completo 8/8 trials en ambos viewports (flujo real: precisión pass-through → go/nogo → transición a stroop); **E2E del fix P1: secuencias de cues no alternantes en vivo** (desktop 4 NO-GO, mobile 3 NO-GO, primera señal GO en ambos); botón ≥44px asertado; fixture report (sin jugar) OK en ambos viewports. Screenshots: `docs/qa/prelaunch-b3-go-nogo/` (15 shots) + vision-verification (cue legible, sin clip, emoji OK en headless, "15 s" visible en header).
- [x] Fixture válido: `?fixture=1` renderiza reporte con card "Control inhibitorio" en ambos viewports
- [x] Constructo resuelto en reporte: `inhibitoryControl` (score provisional, sin baremos, caveats + confidence; señal de inhibición garantizada por piso de NO-GO) — doc módulo §11
- [x] Doc módulo `go_nogo.md` — creada (GNP-P1-2 cerrado)

## Evidencia

- Fix GNP-P1-1: diff en `GoNoGoTask.jsx` (`buildGoNoGoTrials` aleatorizada + `shuffleIndices` con clamp) + 5 tests RED→GREEN en `goNoGo.test.jsx` (propiedades con LCG seed, piso con rng→1, tope con rng→0, counts 1/2, secuencia forzada en tests de componente).
- Fix GNP-P2-1/P2-2: `summarizeGoNoGoResults` (+ `completedTrialCount`, `meanReactionTimeMs`, `goTrialCount`, `noGoTrialCount`) + `computePostErrorSlowingMs` emitido en evento `response` + test de agregación en `gameTelemetry.test.js`.
- Fix GNP-P2-4/P2-5: `height` prop + ITI jitter + test de límites (74/126 ms).
- Fix GNP-P3-1..4/P3-6: `durationLabel '15 s'` (config), `game_end` 0-100, `difficulty` unificada, `noWrap` eliminado, guard `emittedForRef` (StrictMode) + tests.
- Contrato `game_end`: `grep -ln 'game_end' src/tasks/*.jsx` → 6/6 tasks stable_dg presentes (SimpleRT cerrado en B.1).
- Smoke: `node scripts/smoke-b3-go-nogo-2026-09-12.mjs` → `{failures: [], consoleErrors: [], ok: true}` (2026-09-12, Vite dev 127.0.0.1:5173, chromium-1234 headless).
- Viewport medido: `getPostulationGameViewport` → 520×290 @1280×720; 312×340 @390×844 (código + smoke).
- Privacy: `trials` del `onComplete` eliminado por `stripForbidden` (`ASSESSMENT_FORBIDDEN_KEYS` incluye `trials`) y re-validado por `validateFinalAssessmentSessionPrivacy`/`validateFinalAssessmentPayload` (recursivo); suite focal incluye `postulationDemoSessionBuilder` (7/7) y `finalAssessmentPayload` (2/2).
