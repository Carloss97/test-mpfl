# Auditoría pre-lanzamiento — SimpleRT (simple_rt)

**Fecha:** 2026-09-12
**Auditor:** Hermes (FASE B, KRU-116)
**Estado:** **P1 cerrado (game_end) + P2/P3 pendientes de decisión**
**Rollup:** `docs/qa/prelaunch-rollup.md`

---

## Contexto

- Batería: `stable_dg` — bloque **warmup** (`visible: false`, `trialCount: 4`, label "Calentamiento de reacción"). En `batteryConfig.js` (assessment) también existe con `trialCount: 10` (eval) para el flujo de práctica.
- Código: `src/tasks/SimpleRTTask.jsx` (259→267 líneas). Tests: `src/tasks/SimpleRTTask.gameTelemetry.test.jsx`.
- Render en batería: `PostulationGameStage` le pasa `width/height` desde `getPostulationGameViewport()` (clamp 240..620 × 280..340; compact ≤1366/≤820).

## Veredicto por dimensión (8)

| # | Dimensión | Veredicto | Evidencia |
|---|-----------|-----------|-----------|
| 1 | Toma de muestras | ✅ PASS (1 P2) | `pointerSampler` v1: in-memory, `maxSamples: 600` por trial, reset por trial en `doStartTrial`. `summarizePointerTrial` → kinemáticas agregadas. Test aserta `rawPointerPathStored: false, aggregateOnly: true` y payload sin `samples`. **P2:** sin throttling — cada `pointermove` hace `getBoundingClientRect()` + append (ver dim. 8). |
| 2 | Telemetría | ✅ PASS (P1 cerrado hoy) | Eventos: `game_start` ✓, `stimulus_shown` ✓ (posición + radio), `response` ✓ (rt, outcome, pointerSummary) — **`game_end` FALTANTE → FIX aplicado y testeado (2026-09-12)**. Contrato `game_event_v1` vía `normalizeGameEvent` ✓. Legacy `onTrialStart/onTrialEnd` intactos ✓. |
| 3 | Sincronización | ✅ PASS | Máquina `idle→target→feedback→(next\|finished)`. Timeouts: `MAX_RT_MS 3000` (timeout → `doEndTrial(false,null,true)`), ITI aleatorio 600–1000 ms. Cleanup en desactivación, unmount y botón "Repetir" (clara de timeoutId+itiId + reset de estado). Clicks fuera de fase `target` ignorados (phase guard). `stateRef` — sin race por re-render. |
| 4 | Gameplay | ✅ PASS (2 P2) | 10 trials eval / 4 warmup. Feedback por trial (rt ms, ✓/✗, ⏰ timeout). Resumen final: precisión, RT medio, 1ª/2ª mitad (aprendizaje). **P2-1:** hit radius 30 px vs visual radius 25 px (div 50×50) — hitbox generosa 5 px no documentada. **P2-2:** margen fijo 60 px en `randomPos` consume 50% del área en canvas móvil (240 px) → escalar proporcionalmente. |
| 5 | Inferencia | ⚠️ P1 (doc) | `skill: 'processing_speed'` en config. **No existe `docs/design/modulos/simple_rt.md`** (chain R-6: constructo → demanda → conducta → telemetría → feature → regla → disponibilidad). Solo los 7 juegos original tienen doc módulo. El warmup no tiene flag explícito aguas abajo (ver P2-3). Feature keys `simple_rt.*` NO encontradas en `assessment_feature_vector_v2` — el payload v3 lleva `gameResults` (summary por juego) + `talentProfile`; para la batería original el mapping es `originalGameTalentMapping.js`. **Decisión pendiente:** si el warmup aporta evidencia de processing_speed en el reporte, debe etiquetarse `calibration` y no contar como prueba del constructo. |
| 6 | Assets | ✅ PASS | Cero assets externos (divs + CSS). CSP-safe. Emoji 🎯 en título (ver P3-1). Sin SFX en stable_dg (toggle global de GameStage no aplica) — consistente con diseño actual. |
| 7 | Responsividad | ✅ PASS (1 P2) | Viewport responsive del stage ✓ (240×280 mínimo jugable). Target visual 50 px ≥ 44 px AA touch ✓. Sin overflow conocido (margen 60 px lo asegura, aunque es el P2-2). Smoke 2 viewports: **pendiente (hoy)** — ver §Evidencia. |
| 8 | Fluidez | ✅ PASS (1 P3) | Sin rAF loop (timeout-driven). React re-render por trial, no por frame. **P3:** `getBoundingClientRect()` por `pointermove` — cachear rect por trial (cambia solo con scroll/resize; en el stage el canvas está fijo durante el trial). |

## Hallazgos

| ID | Sev | Dim | Descripción | Acción |
|----|-----|-----|-------------|--------|
| SRT-P1-1 | P1 (cerrado) | 2 | `game_end` no emitido — único task de los 6 de stable_dg sin él (ColorInterference, GoNoGo, PrecisionTargeting, PursuitTracking, VisualSearch sí emiten) | **FIX 2026-09-12:** emisión en rama de completado (antes de `onComplete`), patrón GoNoGo: `gameState {score: accuracy*100, level: trialCount, difficulty: 'baseline', combo: 0}` + test de regresión (1 evento, tras último response, sin raw samples). 34/34 suite focal. |
| SRT-P1-2 | P1 | 5 | Sin doc módulo R-6 (`docs/design/modulos/simple_rt.md`) | Crear con chain completa (plantilla `plantilla-modulo-original-game.md` adaptada): constructo processing speed, demanda (RT motor visual), conductas (RT, precisión, aprendizaje 1ª/2ª mitad, kinemática puntero), features, reglas provisionales, disponibilidad (warmup = calibration), caveats. |
| SRT-P2-1 | P2 | 4 | Hit radius 30 ≠ visual radius 25 | Decisión: igualar hitbox al visual (30→25) o documentar hitbox generosa en spec + doc módulo. Recomendado: documentar (generosidad favorece RT limpio en móvil). |
| SRT-P2-2 | P2 | 4/7 | Margen fijo 60 px en `randomPos` | `m = Math.min(60, Math.floor(Math.min(w,h) * 0.15))` — en 240 px → 36 px. Test: target siempre dentro de canvas en viewports 240/390/720. |
| SRT-P2-3 | P2 | 5 | Warmup sin flag aguas abajo en `sanitizeGameResults` | Añadir `role: block.visible === false ? 'calibration' : 'scored'` en `finalAssessmentPayload.sanitizeGameResults` + test. Reporte: no usar calibration como evidencia del constructo. |
| SRT-P3-1 | P3 | 6/4 | Unicode ✓/✗/⏰/🎯 en juego | Verificar en dispositivo real (móvil Android/iOS): si tofu → SVG inline (patrón `t_25009e33` del landing). |
| SRT-P3-2 | P3 | 8 | `getBoundingClientRect` por pointermove | Cachear rect por trial (recomputar en resize). Micro-opt; validar con 30 trials. |

## Gates de pre-lanzamiento (checklist del rollup)

- [x] Tests focales GREEN (34/34: SimpleRT telemetry + batteryRuntime + finalAssessmentPayload + assessmentExperienceSmoke + PostulationGameStage)
- [x] oxlint 0 (SimpleRTTask.jsx — verificar en commit)
- [x] Build OK (verificar en commit)
- [ ] Smoke 2 viewports (1280×720 + 390×844) en navegador: juego completo del warmup 4 trials, sin console errors, sin overflow, feedback por trial legible — **pendiente**
- [x] Fixture válido: warmup no visible en progreso (Game X of N lo omite) — verificado en config (visible:false + listVisiblePostulationBlocks)
- [ ] Constructo en reporte: **decisión SRT-P1-2/SRT-P2-3** (role calibration) — pendiente
- [ ] Doc módulo `simple_rt.md` — pendiente (SRT-P1-2)

## Evidencia

- Fix game_end: diff en `SimpleRTTask.jsx` (rama de completado) + 2do test en `SimpleRTTask.gameTelemetry.test.jsx`.
- Suite focal: `NODE_ENV=test npx vitest run src/tasks/SimpleRTTask.gameTelemetry.test.jsx src/assessment/batteryRuntime.test.js src/assessment/finalAssessmentPayload.test.js src/assessment/assessmentExperienceSmoke.test.js src/postulation-demo/PostulationGameStage.test.jsx` → **34 passed (5 files)**, 2026-09-12.
- Verificación de contrato game_end por task: `grep -ln 'game_end' src/tasks/*.jsx` → 5 de 6 (SimpleRT faltante, cerrado hoy).
- Pointer sampler: `src/telemetry/pointerSampler.js` (in-memory, privacy flags), `summarizePointerTrial` agregado.
- Viewport: `getPostulationGameViewport` clamp 240..620 × 280..340.
