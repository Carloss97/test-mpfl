# Auditoría pre-lanzamiento — PrecisionTargeting (precision_targeting)

**Fecha:** 2026-09-12
**Auditor:** Hermes (FASE B.2, KRU-116)
**Estado:** **✅ PASS — P1×2 y P2×1 cerrados en esta card; P2×2 y P3×9 documentados con decisión**
**Rollup:** `docs/qa/prelaunch-rollup.md` (fila 2)
**Doc módulo R-6:** `docs/design/modulos/precision_targeting.md` (creada en esta card)

---

## Contexto

- Batería: `stable_dg` — bloque **visible** (juego 1 de 4 en "Juego X de N"), `skill: 'visuomotor_precision'`, `trialCount: 4`, `durationLabel: '1 min'`. En `batteryConfig.js` (assessment/UnifiedGameBattery) existe con `trialCount: 8` (standardized) / 4 (demo).
- Código: `src/tasks/PrecisionTargetingTask.jsx` (425→468 líneas). Tests: `src/tasks/precisionTargeting.test.jsx` (7→11 tests). Telemetría shared: `src/telemetry/kinematics.js`, `pointerSampler.js`, `gameTelemetry.js`, `gameFeatureVector.js`.
- Render en batería: `PostulationGameStage` pasa `width/height` desde `getPostulationGameViewport()` (clamp 240..620 × 280..340 compact; medido 520×290 @1280×720 y 312×340 @390×844) + `trialCount` del bloque.
- Mecánica: Fitts — tap start pad central → alcanzar blanco (4 radios cíclicos 34/26/20/16 px, órbitas deterministas) con click/tap; score por trial = f(error espacial); corridor ideal visual; feedback strip por trial.

## Veredicto por dimensión (8)

| # | Dimensión | Veredicto | Evidencia |
|---|-----------|-----------|-----------|
| 1 | Toma de muestras | ✅ PASS (1 P3) | Sampler in-memory `pointer_sampler_v1` cap 900, reset por trial (effect + `beginMovement`), muestra sintética de origen + 1 por pointermove + endpoint. Clock único `performance.now()` (pitfall 23 ✓). `summarizePointerTrial` filtra `[shownAt, responseAt]`. **P3:** cap 900 descarta muestras antiguas en movimiento continuo >~10 s (kinemáticas sobre ventana retenida; documentado). **NUEVO (fix P1-1):** `MIN_KINEMATICS_SAMPLES=3` — con <3 muestras (tap touch) la trayectoria es degenerada (recta trivial, eficiencia 1.0) → `measurable:false` + label neutro `Ruta registrada` + counts agregados + quality flag + caveat (dim. 5). |
| 2 | Telemetría | ✅ PASS | Eventos: `game_start` ✓ (GameRuntime, 1× por montaje), `stimulus_shown` ✓ (geometría Fitts por trial), `response` ✓ (fitts + adaptivePrecision + pointerSummary + endpoint único sanitized), `game_end` ✓ (línea 319, antes de `onComplete` — **verificado**: ya se emitía, note (b) de la card). Contrato `game_event_v1` vía `normalizeGameEvent` ✓ (`privacy.rawPointer:false`). Privacidad: `trials` del summary `onComplete` **se elimina** en `stripForbidden` (`ASSESSMENT_FORBIDDEN_KEYS` incluye `trials`) y se re-valida recursivamente en `validateFinalAssessmentPayload`; block result final = solo `{gameId,totalTrials,accuracy,meanScore,meanRT,meanPathEfficiency}`. Tests asertan payload sin `samples`/`trials`. |
| 3 | Sincronización | ✅ PASS (1 P2 cerrado, 1 P3 cerrado) | Máquina `ready→target→feedback` con phase guards en los 3 handlers (clicks fuera de fase = no-op; double/multi-touch seguro: eventos discretos + batch). ITI 450 ms. **P2-1 (cerrado):** `useMemo(trials,[width,height,trialCount])` + effect de reset con dep de la identidad del objeto `trial` → re-medición del viewport a mitad de trial (resize/rotación/URL-bar) reiniciaba el trial en curso (sampler reset, phase→ready, `stimulus_shown` huérfano/duplicado al re-tapear). Fix: geometría bloqueada al montar (`initialSizeRef`) + canvas a tamaño inicial + test de regresión. **P3-4 (cerrado):** timeout ITI sin cleanup en unmount → `itimerRef` + cleanup. **P2-2 (decisionado):** sin timeout por trial — por diseño (Fitts throughput mide tiempo real de completar; un timeout contaminaría la métrica principal); documentado en doc módulo §7. |
| 4 | Gameplay | ✅ PASS (3 P3) | 4 trials demo / 8 standardized. Hit test = distancia click→centro ≤ radio visual (sin hitbox oculta; diff. SRT-P2-1 ✓). Feedback por trial (status strip, no intrusivo). Resumen final accuracy/pathEfficiency. Cualquier click cierra el trial (miss si fuera) → **sin estados irresolubles** (verificado también tras el lock de geometría: clip residual por encogimiento del contenedor se resuelve con un click). `durationLabel '1 min'` razonable para 4 trials (20–60 s típicos). **P3-6:** target mínimo 32 px (radio 16) < 44 px guideline, ≥ 24 px WCAG 2.5.8 AA — trade-off Fitts decisionado. **P3-1:** interacción núcleo solo-puntero (start pad es `<button>` keyboard-operable; no hay equivalente de teclado para alcanzar el blanco — por diseño, documentado). **P3-5:** sin detección de focus-loss (tab switch infla RT; sistemático en stable_dg). |
| 5 | Inferencia | ✅ PASS (2 P1 cerrados, 2 P2 decisionados/abierto-sistemático) | `skill: 'visuomotor_precision'` declarado en ambas configs ✓. Chain R-6 completa y trazada en el doc módulo: demanda Fitts → conductas (RT, error endpoint, trayectoria) → telemetría agregada (`motor.*` + `fitts.*` en `summarizeGameEvents`) → feature `assessment_feature_vector_v2` (`game.fittsMeanIndexDifficulty`, `game.fittsThroughput`, `pointer.pathEfficiencyMean/overshootRate/jerkMean/correctionRate`) → dimensión `visuomotorPrecision` (rawScore + confidence + caveats) → reporte (TalentDimensionCard, score provisional sin baremos). **P1-1 (cerrado, fix RED→GREEN):** kinemáticas degeneradas en touch leídas como "ruta perfecta" — inflaba la evidencia del constructo en móvil (dispositivo principal del piloto) y el feedback decía "Ruta precisa" en cada tap. Fix completo (label neutro + counts + quality flag + caveat + penalización de confidence) con smoke E2E touch verificando el label en vivo. **P1-2 (cerrado):** sin doc módulo (P1 sistemático de los 5 stable_dg) → `docs/design/modulos/precision_targeting.md` creada con chain completa. **P2-3 (decisionado):** `visuomotorPrecision` mezcla `smoothPursuitScore` (=0 en stable_dg: pursuit no está en esta batería) → sesgo constante nondiscriminativo; fix en mapeo R-6 validado post-B. **P2-4 (abierto sistemático, SRT-P2-3):** `sanitizeGameResults` sin `role: calibration|scored` — impacto bajo aquí (juego scored, visible:true). |
| 6 | Assets | ✅ PASS (1 P3) | Cero assets externos (divs + SVG inline + CSS). CSP-safe. Sin SFX en stable_dg (toggle global de GameStage visible pero inerte para este juego — consistente con diseño actual). **P3-3:** emoji 🎯 en título (verificación real-device pendiente, patrón `t_25009e33`; SFX toggle 🔊/🔇 mismo). |
| 7 | Responsividad | ✅ PASS (1 P3) | Smoke 2 viewports 2026-09-12: **0 overflow horizontal** en 1280×720 y 390×844 (page.evaluate scrollWidth ≤ innerWidth+1 en cada pantalla: setup, stage ready, por trial, transición go/no-go, fixture report ×2). Start pad 56 px ≥ 44 AA ✓ (asert smoke). Canvas 520×290 desktop / 312×340 móvil (medido). Route card 4 celdas legible en móvil (vision-verified, sin overlap/clip). `task-area { max-width:100%; overflow:hidden }` contiene. **P3-9:** canvas alineado izquierda en desktop ancho (vacío derecho) — cosmético, sin fix en B.2. |
| 8 | Fluidez | ✅ PASS (1 P3 revisado) | Sin rAF loop (event-driven); re-render React por trial, no por frame. **P3-2 (revisado, note (c) de la card):** `getBoundingClientRect()` por `pointermove` — **se mantiene por-evento**: cache por trial podría volverse stale con scroll interno del stage (`postulation-demo__game-stage { overflow:auto }`) → coordenadas erróneas silenciosas; sin layout thrash (read sin write en el mismo frame). Misma decisión que SRT-P3-2. ITI 450 ms (timer, no bloquea). SFX: no aplica (stable_dg). |

## Hallazgos

| ID | Sev | Dim | Descripción | Acción |
|----|-----|-----|-------------|--------|
| PRE-P1-1 | P1 (cerrado) | 5/1 | **Kinemáticas degeneradas en touch:** un tap produce exactamente 2 muestras (origen sintético + click) → `pathEfficiency=1.0`, jerk/correction/overshoot=0 → feedback "Ruta precisa" y evidencia `visuomotorPrecision` inflada y no discriminativa en móvil (violación R-6: señal ausente/degenerada = caveated, nunca desempeño perfecto) | **FIX 2026-09-12 RED→GREEN:** `MIN_KINEMATICS_SAMPLES=3` (kinematics.js); `adaptivePrecision.measurable` + label neutro `Ruta registrada` (ES/EN); `motor.kinematicsResponseCount/kinematicsMeasuredCount` (gameTelemetry); `qualityFlags += kinematics_insufficient_samples` (gameFeatureVector); caveat en `qualitySummary` → dimensión + `qualityConfidence` −0.08 (postulationDemoSessionBuilder). 5 tests RED→GREEN (34/34 focal) + **smoke E2E: label neutro visible en vivo en 4/4 trials touch** y labels de calidad en mouse. |
| PRE-P1-2 | P1 (cerrado) | 5 | Sin doc módulo R-6 (`docs/design/modulos/precision_targeting.md`) — P1 sistemático: los 5 stable_dg no tenían doc | **Creado en esta card** (template adaptada; chain completa §0–§15 + trazabilidad de auditoría §16 + pitfalls §17). |
| PRE-P2-1 | P2 (cerrado) | 3 | Reset de trial a mitad de carrera por re-medición de viewport: `trials` memo dependía de `width/height` (stage re-mide en resize/rotación/visualViewport) y el effect de reset dependía de la identidad del objeto trial → sampler reset + phase→ready + `stimulus_shown` huérfano (duplicado al re-tapear) | **FIX 2026-09-12:** geometría + canvas bloqueados al montar (`initialSizeRef`); test de regresión (resize mid-trial: target inmutable, pad disabled, 1 stimulus). Residual documentado: si el contenedor se encoge bajo el tamaño de mount, clip de blancos en el borde — cualquier click resuelve (miss), sin hang. **Nota para B.5:** `visual_search` tiene el mismo patrón (`buildVisualSearchTrials` useMemo [width,height,trialCount], línea 138) — re-verificar. |
| PRE-P3-4 | P3 (cerrado) | 3 | Timeout ITI 450 ms sin cleanup en unmount (race latente; inofensivo en flujo actual porque el trial final no programa timeout y la salida del stage es navegación total) | **FIX 2026-09-12:** `itimerRef` + cleanup en unmount. |
| PRE-P2-2 | P2 (abierto, decisionado) | 3/4 | Sin timeout por trial: fase `target` sin bound → meanRT/throughput incluye deliberación; sin outcome `omission` | Decisión: **por diseño** — el throughput de Fitts mide el tiempo real de completar el movimiento; timeout global contaminaría la métrica principal. Documentado en doc módulo §7. Sin cambio de código en B.2. |
| PRE-P2-3 | P2 (abierto, decisionado) | 5 | Dimensión `visuomotorPrecision` mezcla `smoothPursuitScore` (0 en stable_dg: `pursuit_tracking` no está en la batería demo) + `mean()` colapsa null→0 (no hay representación de señal ausente en componentes rawScore) | Decisión: **sesgo constante nondiscriminativo** (mismo offset para todos los candidatos); aceptado como provisional descriptivo; la corrección (pesos por juego validados / null-safe) pertenece al mapeo R-6 post-FASE B, no a un hotfix por juego. Documentado en doc módulo §11. |
| PRE-P2-4 | P2 (abierto, sistemático) | 5 | `sanitizeGameResults` no expone `role: calibration\|scored` (SRT-P2-3, B.1) | Impacto bajo para este juego (`visible:true` = scored; el warmup simple_rt es quien lo necesita). Fix coordinado con la card B.1 / epic FASE B; rastreado en rollup. |
| PRE-P3-1 | P3 (decisionado) | 7 | Keyboard: interacción núcleo solo-puntero; start pad es `<button>` (Enter/Space inicia trial) | Por diseño (tarea Fitts de puntero); documentado en doc módulo §7. |
| PRE-P3-2 | P3 (revisado, decisionado) | 8 | `getBoundingClientRect()` por `pointermove` (SRT-P3-2) | Se mantiene por-evento (correctitud > micro-opt: cache stale con scroll interno del stage); sin layout thrash (verificado). Misma decisión que SRT-P3-2. |
| PRE-P3-3 | P3 (abierto, sistemático) | 6 | Emoji 🎯 en título (+ SFX toggle 🔊/🔇 shared) | Verificación real-device pendiente (patrón `t_25009e33`); si tofu → SVG inline. |
| PRE-P3-5 | P3 (abierto, sistemático) | 4 | Sin detección de focus-loss (blur infla RT silenciosamente) | Igual que resto de stable_dg (plan B.2.1 `blurCount` no implementado en ningún juego stable_dg). |
| PRE-P3-6 | P3 (decisionado) | 7 | Target mínimo 32 px < 44 px AA guideline (≥ 24 px WCAG 2.5.8) | Trade-off Fitts decisionado; hit test por distancia al centro (generoso en borde). Documentado. |
| PRE-P3-7 | P3 (abierto, sistemático CSS shared) | 7 | Animaciones `.rt-target` (150 ms) / `.trial-feedback` (700 ms) en `styles.css` sin gate `prefers-reduced-motion` | Afecta a todos los stable_dg; fix coordinado (design system). |
| PRE-P3-8 | P3 (abierto, documentado) | 1 | Cap 900 del sampler descarta muestras antiguas en movimiento >~10 s continuo (pierde muestra de origen sintética) | Cap de protección de memoria; kinemáticas sobre ventana retenida. Documentado. |
| PRE-P3-9 | P3 (abierto) | 7 | Canvas alineado a la izquierda en desktop ancho (vacío a la derecha) | Cosmético; centrar `task-area` (recomendado, sin fix en B.2). |

## Gates de pre-lanzamiento (checklist del rollup)

- [x] Tests focales GREEN: `precisionTargeting` (11) + `kinematics` (5) + `gameTelemetry` (6) + `gameFeatureVector` (5) + `postulationDemoSessionBuilder` (7) = **34/34** (5 files). Regresión en 19 files relacionados (SimpleRT telemetry, gameRerenderStability, talentProfile, finalAssessmentPayload, batteryRuntime, assessmentExperienceSmoke, assessmentSession, PostulationGameStage, PostulationReportScreen, PostulationQaFixes, pointerSampler, kinematics.v2, simulatedGameSessions, gamePayload, localSignalSync, fixture, participantAssessmentFlow, FlowDesignSystem, DemoApp) = **122/122**. Suite completa: ver §Evidencia.
- [x] oxlint 0 en archivos tocados (v1.81.0; scope completo AGENTS.md: 1 warning preexistente en `ParticipantAssessmentFlow.jsx`, no tocado).
- [x] Build OK (`npm run build`, 5.7 s; warning de chunk size preexistente).
- [x] Smoke 2 viewports (1280×720 mouse + 390×844 touch) en Vite dev + Playwright — `scripts/smoke-b2-precision-targeting-2026-09-12.mjs`: **0 failures, 0 console errors, 0 requestfailed, 0 overflow**; precisión jugada 4/4 trials en ambos viewports (práctica completatable: flujo real, no fixture); transición a go/no-go íntegra; fixture report (sin jugar) OK en ambos viewports; **E2E del fix P1: "Ruta registrada" en vivo en touch / labels de calidad en mouse**. Screenshots: `docs/qa/prelaunch-b2-precision/` (8 shots).
- [x] Fixture válido: `?fixture=1` renderiza reporte con card de precisión (stable_dg, 4 juegos visibles).
- [x] Constructo resuelto en reporte: `visuomotorPrecision` (score provisional, sin baremos, caveats + confidence; kinemáticas touch caveadas vía flag/quality) — doc módulo §11.
- [x] Doc módulo `precision_targeting.md` — creada (PRE-P1-2 cerrado).

## Evidencia

- Fix P1-1: diff en `PrecisionTargetingTask.jsx` (`measurable` + label neutro), `kinematics.js` (`MIN_KINEMATICS_SAMPLES`), `gameTelemetry.js` (counts motor), `gameFeatureVector.js` (quality flag), `postulationDemoSessionBuilder.js` (caveat + reorder de featureVectorV2). Tests RED→GREEN: 7 tests nuevos fallando antes del fix (5 files), 34/34 después.
- Fix P2-1 + P3-4: `initialSizeRef` (lock geometría al montar) + `itimerRef` (cleanup ITI) en `PrecisionTargetingTask.jsx`; test "does not reset an in-flight trial when the stage re-measures the viewport mid-trial".
- Contrato `game_end`: `grep -ln 'game_end' src/tasks/*.jsx` → presente en PrecisionTargeting (línea 319 post-fix; ya existía — note (b) verificado).
- Smoke: `node scripts/smoke-b2-precision-targeting-2026-09-12.mjs` → `{failures: [], consoleErrors: [], ok: true}` (2026-09-12, Vite dev 127.0.0.1:5173, chromium-1234 headless).
- Vision-verification de screenshots: desktop (feedback "Ajuste fino requerido · Ruta con correcciones · 1003ms · error 1.2px · score 99%" — trayectoria real con bend del smoke = correcciones honestas) y móvil (feedback "Precisión estable · **Ruta registrada** · 396ms · error 0.5px · score 100%" — label neutro en vivo; route card 4 celdas legible; sin clip/overlap/overflow).
- Viewport medido: `getPostulationGameViewport` → 520×290 @1280×720; 312×340 @390×844 (código + smoke).
- Privacy: `trials` del `onComplete` eliminado por `stripForbidden` (`ASSESSMENT_FORBIDDEN_KEYS` line 25) y re-validado por `validateAssessmentSessionPrivacy` (recursivo) en session y final payload; tests existentes asertan `result` sin `trials` + `payload.validation.ok`.
