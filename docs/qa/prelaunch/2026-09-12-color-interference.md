# Auditoría pre-lanzamiento — ColorInterference (color_interference)

**Fecha:** 2026-09-12
**Auditor:** Hermes (FASE B.4, KRU-116)
**Estado:** **✅ PASS — P1×2 y P2×6 cerrados en esta card (RED→GREEN + smoke E2E); P3×3 abiertos sistemáticos + P3×1 decisionado**
**Rollup:** `docs/qa/prelaunch-rollup.md` (fila 4)
**Doc módulo R-6:** `docs/design/modulos/color_interference.md` (creada en esta card)

---

## Contexto

- Batería: `stable_dg` — bloque **visible** (juego 3 de 4 en "Juego X de N"), `skill: 'interference_control'`, `trialCount: 8`, `durationLabel: '1 min'` → **fix a `'30 s'`** (CIP-P3-1: 8 trials × ~1–3.45 s ≈ 10–28 s reales). En `batteryConfig.js` (assessment) también existe con `trialCount: 24` (standardized) / 8 (demo) — flujo legacy, sin cambios (mismo tratamiento que B.1–B.3).
- Código: `src/tasks/ColorInterferenceTask.jsx` (355→430 líneas). Tests: `src/tasks/colorInterference.test.jsx` (9→18 tests). Telemetría shared: `gameTelemetry.js` (+2 tests). `colorStimulusLayout.js` (+test, 4/9 líneas) → **eliminado (código muerto, CIP-P3-8)** — el layout real estaba inline en el componente (`classifyStimulusWordLength` + CSS de la card).
- Render en batería: `PostulationGameStage` pasa `width/height` desde `getPostulationGameViewport()` (520×290 @1280×720 y 312×340 @390×844 — mismo stage que B.2/B.3) + `trialCount` del bloque. **`height` era ignorada (min-height 260 fijo) → fix CIP-P2-4.**
- Mecánica: Stroop color-palabra — elegir el color real de la tinta ignorando el texto; 4 opciones (Rojo/Azul/Verde/Amarillo); ventana 3200 ms por trial; timeout = error.

## Veredicto por dimensión (8)

| # | Dimensión | Veredicto | Evidencia |
|---|-----------|-----------|-----------|
| 1 | Toma de muestras | ✅ PASS (1 P2 cerrado) | 1 respuesta por trial (click discreto, sin pointer sampler — note (f) N/A). Clock único `performance.now()` (pitfall 23 ✓; nota: `shownAtRef` se fija en el effect post-paint → sesgo de montaje ~1 frame, sub-segundo y constante, documentado en doc §7). Reset por trial en el effect (`handledRef`/feedback/`remainingMs`). **P2-1 (cerrado):** el summary usaba `totalTrials` solo — sin las claves de contrato del session builder (`completedTrialCount`, `meanReactionTimeMs`) → la RT de color_interference no entraba a la media de batería y el "Tiempo" del reporte aparecía **ausente** (`getDefaultGameMetrics` → null → skip). Fix: claves añadidas + test. Calidad: piso ≥2 por condición (fix P1-1) garantiza denominador ≥2 en las 3 condiciones en batería de 8. |
| 2 | Telemetría | ✅ PASS (P2 cerrado + 2 P3 cerrados) | Eventos: `game_start` ✓ (GameRuntime), `stimulus_shown` ✓ (wordEs/wordEn/wordColor/ink/condition — metadata, sin coordenadas), `response` ✓ (outcome, RT, bloque `interference` con **`condition`** — fix CIP-P2-2), **`game_end` ✓ — verificado (note (d)): ya se emitía** (rama de completado, antes de `onComplete`). Contrato `game_event_v1` vía `normalizeGameEvent` ✓. Privacidad: `trials` del summary **se elimina** en `stripForbidden` (`ASSESSMENT_FORBIDDEN_KEYS` incluye `trials`) + re-validación recursiva en `validateFinalAssessmentPayload` ✓ (suite focal: session builder 7/7 + finalAssessmentPayload 2/2). **P2-2 (cerrado):** `summarizeGameEvents.interference` clasificaba solo por el booleano `congruent` (sin condición neutral) y `conflictCostMs = incongruente − congruente` (no canónico) sin guard → costo negativo con RT ausente. Fix: clasificación por `condition` con fallback al booleano legado + `neutralRT`/`neutralAccuracy` + **costo = incongruente − neutral con guard no-negativo** + 2 tests. **P3-2 (cerrado):** `game_end.gameState.score` 0–1 → `Math.round(meanScore*100)`. **P3-3 (cerrado):** `difficulty` inconsistente (`conflict` en game_start / `congruent`-`incongruent` por trial / `mixed_interference` en game_end) → unificada a `color_interference` en `gameState`; la condición vive solo en el payload estímulo/respuesta. Nota sistémica (documentada, sin fix): el effect del hijo corre antes que el del padre → `stimulus_shown` precede a `game_start` en TODOS los juegos GameRuntime (B.3). |
| 3 | Sincronización | ✅ PASS (2 P2 + 2 P3 cerrados) | Máquina: estímulo 3200 ms → click/timeout → feedback + ITI → trial+1; todo trial termina (click o timeout) → **sin estados irresolubles/hang**. **P2-3 (cerrado):** los `setTimeout` de ITI (inter-trial y de completado final) no se rastreaban ni se limpiaban → `onComplete` tardío si el flujo se aborta/unmount durante ITI (bloque completado con closure viejo — mismo patrón "ITI cleanup" que B.2). Fix: `itiRef` + cleanup en unmount/cambio de trial + completado inmediato al final (patrón `completeIfFinished` de B.3) + test unmount-during-ITI. **P2-5 (cerrado):** ITI fijo 250 ms → jitter [0.75, 1.25] × itiMs (fix CIP-P2-5; mismo patrón GNP-P2-5) + test de límites (74/126 ms). **P3-4 (cerrado):** StrictMode (dev) duplicaba `stimulus_shown` del trial 0 → guard `emittedForRef` por índice + test. **P3-5 (cerrado):** sin `handledRef` (doble-puntuación teórica en double-tap ultra-rápido) → `handledRef` por trial (patrón B.3) + test de doble-click. Imune al reset por re-medición de viewport (B.2/B.5): `trials = useMemo([trialCount])` — la geometría no puntúa. |
| 4 | Gameplay | ✅ PASS (P1 cerrado + 1 P3 cerrado) | **P1 (CIP-P1-1, cerrado):** estímulo determinista (`TRIAL_PATTERN` 7-ciclo → en 8 trials **C/I/C/I… estricto**) + **la UI anunciaba la condición de cada trial** (pill "Tipo: congruente/incongruente", asertado incluso en el test EN) + **sin condición neutral**: el candidato predecía qué venía y modulaba su estrategia sin conflicto genuino; además `conflictCostMs = incongruente − congruente` no es la métrica Stroop canónica → constructo `interference_control` inválido. Fix: `buildColorInterferenceTrials` aleatorizado (3 condiciones ~1/3, trial 0 congruente, piso ≥2/condición en 8 trials, `rng` inyectable con Fisher-Yates clampado, estímulo **i18n** ES/EN + neutral `XXXXX`) + **retirada del pill "Tipo:"** (regresión testeada ES+EN) + **smoke E2E**: secuencias en vivo con las 3 condiciones, floor cumplido, pregunta 1 congruente, sin anuncio (DOM + vision), sin secuencia legacy. Práctica completatable: flujo real jugado 8/8 trials en smoke (nota: stable_dg no tiene botón de práctica G.2 — micro-bloques + 8 trials; verificado en vivo). Sin tutorial guiado (instrucción fija en pantalla) — OUT documentado (doc §1.3). |
| 5 | Inferencia | ✅ PASS (1 P1 cerrado; 2 P3 abiertos sistemáticos documentados) | `skill: 'interference_control'` declarado en ambas configs ✓. Chain R-6 completa trazada en `docs/design/modulos/color_interference.md` (CIP-P1-2, P1 sistemático stable_dg — cerrado): constructo (control de interferencia) → demanda (Stroop de 3 condiciones aleatorizado, sin anuncio) → conducta (naming de tinta bajo conflicto: RT por condición, errores, costo) → telemetría agregada (`interference.*` con neutral) → feature v2 (`interference.conflictCostMs` ahora canónico, sin reordenar `featureOrder`) → regla provisional (dimensión `interferenceControl`, `descriptive_only`) → disponibilidad/confianza/caveats → narrativa de revisión humana. Reporte honesto: **score provisional sin baremos** + confidence + caveats; la señal existe por construcción (piso ≥2/condición). **P3-11 (abierto sistemático):** `talentProfile` sin camino null-score por dimensión — sesión interrumpida antes del juego 3 → `conflictCostMs 0` → `conflictScore(0) = 1` (máximo) → se leería "alta" en lugar de desconocida; mismo GNP-P3-8 de B.3, afecta a todo stable_dg, fix post-B. **P3 (abierto sistemático):** canal EdgeAI `cognitiveLoad` = proxy de agregado (GNP-P3-7 pattern) → mapeo R-6 post-B. |
| 6 | Assets | ✅ PASS (1 P3 abierto sistemático) | Cero assets externos (divs + CSS). CSP-safe. Contraste de tintas de estímulo **medido** (note c): sobre `--k-card-cream` #f2e8dc — rojo 3.99:1, azul 4.27:1, verde 3.11:1, amarillo 4.15:1; estímulo = texto grande (2.5–3rem/900) → umbral AA large 3:1 → **las 4 PASS** (verde al límite: documentado). "Amarillo" usa `#b45309` (ámbar, no amarillo puro) **por diseño AA** — decisionado (doc §5/§14). **P3-6 (abierto):** emoji 🌈 en título — renderiza OK en headless (vision en screenshots de smoke); real-device pendiente (patrón `t_25009e33`); si tofu → SVG inline. SFX: sin SFX propio en stable_dg (toggle global del stage inerte para este juego — consistente, como B.2/B.3). |
| 7 | Responsividad | ✅ PASS (1 P2 cerrado + 1 P3 decisionado) | **P2-4 (cerrado):** `height` prop ignorada (min-height 260 fijo) + card-stage de ~317–350 px > stage 290 px desktop-compact / 340 px móvil → **scroll interno durante trial cronometrado**. Fix: prop `height` (default 300) + CSS compacto (gap 8, padding 12/10, line-height estímulo 1.15, feedback compacto, **grid 4×1 en ≥900px** / 2×2 en móvil) + smoke asert `scrollHeight ≤ clientHeight+1` **por trial, en estímulo y en feedback**, 2 viewports → 0 violaciones. Botones de tinta `min-height: 44px` ≥ 44 AA touch (asert smoke + CSS; 2×2 en móvil = ~145×44). **AMARILLO (8 letras, long-word 2.5rem): 1 línea en 240–312 px de contenido — vision-verified** en desktop y móvil (note e). Teclado: 4 `<button>` focusables → Tab/Enter juega la tarea completa ✓. `prefers-reduced-motion`: sin `@keyframes` propios (solo transición width 120 ms de la timebar, funcional y redundante con el timer numérico) → N/A. Sin overflow horizontal (smoke, ambas viewports). **P3-10 (decisionado):** timebar no-textual 1.4–2.45:1 sobre arena — duplica el timer numérico (6.84:1 AA); el color de estado no es el único canal. |
| 8 | Fluidez | ✅ PASS (note (f) revisada) | Sin rAF loop de puntuación; tick de 100 ms solo para el contador (10 renders/s, ligero); re-render por trial, no por frame. Transición width 120 ms de la timebar (compositor-friendly). **Note (f) SRT-P3-2 (pointer sampler caching): N/A** — color_interference no captura `pointermove` (respuesta discreta en botón, sin pointerSampler) — mismo N/A que B.3 (note d). ITI timers no bloquean hilo principal. Sesión larga: resultsRef ≤ 24 trials, sin buffers crudos. **Pitfall de evidencia:** el primer run del smoke bajo carga (suite completa en paralelo en la Pi) mostró un timeout legítimo en desktop Q1 (round-trips de Playwright ~1 s); re-run con la Pi libre → 0/0 — documentado como pitfall del smoke (§17 del doc), no del juego. |

## Hallazgos

| ID | Sev | Dim | Descripción | Acción |
|----|-----|-----|-------------|--------|
| CIP-P1-1 | P1 (cerrado) | 4/5 | **Secuencia determinista + condición anunciada + sin neutral**: `TRIAL_PATTERN` 7-ciclo → C/I/C/I… estricto en 8 trials; pill "Tipo: congruente/incongruente" telegrafiaba cada trial; sin condición neutral → `conflictCostMs = incongruente − congruente` (no canónico) → constructo `interference_control` inválido | **FIX 2026-09-12 RED→GREEN:** `buildColorInterferenceTrials({count, minPerCondition, rng})` — 3 condiciones ~1/3, trial 0 congruente, piso ≥2/condición (robo solo de condiciones sobre-piso), tintas/palabras aleatorias, `rng` inyectable (Fisher-Yates clampado); estímulo **i18n** (`wordEs`/`wordEn`; EN no tiene color ≥8 letras → clasificación por idioma); neutral `XXXXX`; **retirada del pill "Tipo:"** (regresión ES+EN); `condition` en payload estímulo/respuesta (`congruent: null` en neutral, sin romper filtros legacy). 11 tests nuevos (propiedades, seed/reproducibilidad, counts degenerados, i18n, flujo completo con rng, sin anuncio) + **smoke E2E**: secuencias vivas desktop `[C,N,C,N,I,C,I,C]` (4C/2I/2N) y mobile `[C,I,I,N,C,I,I,N]` (2C/4I/2N) — floor ≥2 cumplido, Q1 congruente, sin anuncio (DOM+vision), sin secuencia legacy |
| CIP-P1-2 | P1 (cerrado) | 5 | Sin doc módulo R-6 (`docs/design/modulos/color_interference.md`) — P1 sistemático: los 5 stable_dg no tenían doc | **Creada en esta card** (template `plantilla-modulo-original-game.md` adaptada; chain completa §0–§17) |
| CIP-P2-1 | P2 (cerrado) | 1/5/12 | Summary sin claves de contrato del session builder (`completedTrialCount`, `meanReactionTimeMs`) → RT de stroop ausente de la media de batería Y del "Tiempo" del reporte (mismo bug GNP-P2-1) | **FIX:** `meanReactionTimeMs` (RT media de aciertos, 3 condiciones) + `completedTrialCount` + test de contrato |
| CIP-P2-2 | P2 (cerrado) | 2/5 | `summarizeGameEvents.interference`: sin condición neutral (los neutral caían en `congruent===false` si se hubieran agregado), costo no canónico (− congruente), sin guard (RT ausente → costo negativo) | **FIX:** clasificación por `condition` con fallback al booleano legado + `neutralRT`/`neutralAccuracy` + costo **incongruente − neutral** con guard no-negativo + 2 tests (canónico 70 ms; guard 0 con legacy boolean) |
| CIP-P2-3 | P2 (cerrado) | 3 | `setTimeout` de ITI (inter-trial y de completado) sin ref/cleanup → `onComplete` tardío tras abort/unmount (bloque viejo completado con datos del siguiente) | **FIX:** `itiRef` + cleanup en unmount/cambio de trial + completado inmediato al final (`completeIfFinished`, patrón B.3) + test unmount-during-ITI |
| CIP-P2-4 | P2 (cerrado) | 7 | `height` prop ignorada (min-height 260 fijo) + card-stage ~317–350 px > stage 290/340 px → scroll interno en trial cronometrado | **FIX:** prop `height` + CSS compacto (gap/padding/line-height, feedback compacto, grid 4×1 en ≥900) + test (312×340) + **smoke fit por trial** (estímulo y feedback, 2 viewports) |
| CIP-P2-5 | P2 (cerrado) | 3/4 | ITI fijo 250 ms → ritmo telegrafiado (GNP-P2-5 pattern) | **FIX:** jitter [0.75, 1.25] × itiMs + test de límites (74/126 ms) |
| CIP-P3-1 | P3 (cerrado) | 4 | `durationLabel: '1 min'` vs 10–28 s reales (8 trials) | Fix: `'30 s'` en `postulationDemoConfig.js` + test (vision: "30 s" visible en header) |
| CIP-P3-2 | P3 (cerrado) | 2 | `game_end.gameState.score` 0–1 (resto: 0–100) | Fix: `Math.round(meanScore*100)` + test (score 100 en 4/4) |
| CIP-P3-3 | P3 (cerrado) | 2 | `difficulty` inconsistente (`conflict`/`congruent`-`incongruent`/`mixed_interference`) | Fix: unificada a `color_interference` en `gameState`; condición solo en payload estímulo/respuesta + test |
| CIP-P3-4 | P3 (cerrado) | 3 | StrictMode (dev): `stimulus_shown` duplicado del trial 0 | Fix: guard `emittedForRef` por índice + test (1 evento, no 2) |
| CIP-P3-5 | P3 (cerrado) | 3/4 | Sin `handledRef` (doble-puntuación teórica en doble-tap) | Fix: `handledRef` por trial (patrón B.3) + test de doble-click |
| CIP-P3-6 | P3 (abierto, sistemático) | 6 | Emoji 🌈 en título (+ SFX toggle 🔊/🔇 shared) | Headless: OK (screenshots). Real-device pendiente (patrón `t_25009e33`); si tofu → SVG inline |
| CIP-P3-7 | P3 (cerrado) | 5 | `conflictCostMs` negativo sin RT de una condición → `conflictScore(0)=1` (máximo) en talentProfile | Fix: guard no-negativo en juego y en `gameTelemetry` + tests. El gap null-score de sesiones interrumpidas queda en CIP-P3-11 (sistemático) |
| CIP-P3-8 | P3 (cerrado) | 12 | `colorStimulusLayout.js` (stub 4 líneas) + test — **código muerto** (0 imports; el layout real está inline) | Eliminado file + test |
| CIP-P3-9 | P3 (abierto, sistemático) | 2 | `sanitizeGameResults` sin `role: calibration\|scored` (SRT-P2-3, note (g)) | Este juego es `visible:true` (scored) → impacto bajo; fix coordinado con B.1/epic (GNP-P3-10) |
| CIP-P3-10 | P3 (decisionado) | 7 | Timebar no-textual 1.4–2.45:1 sobre arena | Duplica el timer numérico (6.84:1 AA) → información disponible con contraste AA; color de estado no es único canal (doc §14) |
| CIP-P3-11 | P3 (abierto, sistemático) | 5 | `talentProfile` sin camino null-score por dimensión: sesión sin datos de stroop → `conflictCostMs 0` → `conflictScore(0)=1` + `1−errorRate(0)=1` → dimensión "alta" sin evidencia (R-6: señal ausente = desconocida) | Mismo GNP-P3-8 (B.3); afecta a todo stable_dg; fix post-B (null-safe por fuente de señal); mitigado por el piso §2 (batería completa) — doc §11 |

## Notas de la card — resolución

- **(a)** Doc módulo creada (`docs/design/modulos/color_interference.md`); constructo verificado: `skill: 'interference_control'` en ambas configs; el reporte lo soporta **honestamente** tras el fix: costo Stroop canónico (vs neutral) + piso ≥2/condición (señal por construcción) + `descriptive_only` sin baremos; residual sistémico (sesión interrumpida) documentado CIP-P3-11.
- **(b)** Balance congruente/incongruente/neutral: **fix CIP-P1-1** (peso ~1/3, piso ≥2, trial 0 congruente, rng inyectable) + orden no predecible verificado E2E (2 secuencias vivas distintas, no-legacy) + condición **sin anunciar** en UI (regresión test + vision).
- **(c)** Nombres de color ES/EN: **fix CIP-P1-1** (estímulo i18n; antes siempre ES — en EN el estímulo dejaba de ser palabra de color válida). Contraste AA medido: 3.99/4.27/3.11/4.15 (large-text 3:1 → PASS; verde al límite documentado; "amarillo" `#b45309` por diseño AA).
- **(d)** `game_end`: **YA se emitía** — verificado (línea de completado, antes de `onComplete`); se normalizó escala 0-100 + difficulty (CIP-P3-2/3) + test de regresión (1 evento, tras último response, sin raw).
- **(e)** Layout 240 px móvil: **AMARILLO (long-word) cabe en 1 línea en 240–312 px de contenido — vision-verified** (desktop y móvil); `colorStimulusLayout.js` era código muerto (CIP-P3-8); el layout real es `classifyStimulusWordLength` + CSS compacto (CIP-P2-4).
- **(f)** Pointer sampler caching (SRT-P3-2): **N/A** — sin `pointermove` (respuesta discreta en botón, sin pointerSampler), como B.3.
- **(g)** Role calibration/scored (SRT-P2-3): juego `visible:true` = scored → **impacto bajo; P3 abierto sistemático** (CIP-P3-9, tracked en rollup con GNP-P3-10).
- **(h)** Ejecutado solo en main, sin otras cards FASE B running (verificado al inicio: única `running`).

## Gates de pre-lanzamiento (checklist del rollup)

- [x] Tests focales GREEN: **134/134 (14 files)** — `colorInterference` (18) + `gameTelemetry` (9) + `postulationDemoConfig` (7) + `gameRerenderStability` + `PostulationGameStage` + `PostulationQaFixes` (7) + `PostulationGamesDesignSystem` (11) + `postulationDemoSessionBuilder` (7) + `batteryRuntime` (4) + `assessmentExperienceSmoke` (2) + `finalAssessmentPayload` (2) + `gameFeatureVector` (5) + `PostulationReportScreen` + `goNoGo` (15, regresión del shared gameTelemetry)
- [x] Regresión completa: **1330/1330 (146 files)**
- [x] oxlint 0 en archivos tocados (`ColorInterferenceTask.jsx`, `colorInterference.test.jsx`, `gameTelemetry.js`, `gameTelemetry.test.js`, `postulationDemoConfig.js`, `postulationDemoConfig.test.js`)
- [x] Build OK (`npm run build`, 9.17 s; warning de chunk size preexistente)
- [x] Smoke 2 viewports (1280×720 mouse + 390×844 touch) en Vite dev + Playwright — `scripts/smoke-b4-color-interference-2026-09-12.mjs`: **0 failures, 0 console errors, 0 requestfailed, 0 overflow**; stroop jugado completo 8/8 trials en ambos viewports (flujo real: precisión → go/nogo → **stroop** → transición a Juego 4 de 4); **E2E del fix P1: secuencias vivas con las 3 condiciones, floor ≥2 cumplido, pregunta 1 congruente, sin anuncio de condición, sin secuencia legacy**; fit del stage (sin scroll interno) asertado por trial (estímulo + feedback); botones ≥44px; fixture report OK en ambos viewports (card "Interferencia cognitiva" presente). Screenshots: `docs/qa/prelaunch-b4-color-interference/` (9 shots) + vision-verification (AMARILLO 1 línea, "30 s" visible, sin "Tipo:", 4 botones en fila desktop / 2×2 móvil, report con watermark de integridad ≠ validez).
- [x] Fixture válido: `?fixture=1` renderiza reporte con card "Interferencia cognitiva" en ambos viewports
- [x] Constructo en reporte: `interferenceControl` (score provisional, sin baremos, caveats + confidence; señal por construcción vía piso; residual sistémico CIP-P3-11 documentado) — doc módulo §11
- [x] Doc módulo `color_interference.md` — creada (CIP-P1-2 cerrado)

## Evidencia

- Fix CIP-P1-1: diff en `ColorInterferenceTask.jsx` (`buildColorInterferenceTrials` aleatorizado 3 condiciones + piso + rng + i18n estímulo + retirada del pill "Tipo:" + `condition` en payload) + 11 tests RED→GREEN en `colorInterference.test.jsx` (propiedades con LCG seed, piso, reproducibilidad, counts pequeños, flujo completo con rng inyectado, ausencia de anuncio ES+EN, estímulo EN).
- Fix CIP-P2-2/CIP-P3-7: `gameTelemetry.js` (clasificación por `condition` con fallback legacy, `neutralRT`/`neutralAccuracy`, costo vs neutral con guard) + 2 tests en `gameTelemetry.test.js`.
- Fix CIP-P2-1: `summarizeColorInterferenceResults` (+ `completedTrialCount`, `meanReactionTimeMs`, `neutralRT`, `facilitationMs`, `conditionCounts`) + test de contrato.
- Fix CIP-P2-3/P2-5/CIP-P3-4/CIP-P3-5: `itiRef` + cleanup + jitter + `emittedForRef` + `handledRef` + tests (unmount-during-ITI, límites 74/126 ms, StrictMode, doble-click).
- Fix CIP-P2-4/P3-1: prop `height` + CSS compacto (postulationDemo.css: gap 8, padding 12/10, line-height 1.15, feedback compacto, grid 4×1 ≥900px, option 44px) + `durationLabel: '30 s'` (config + test) + smoke fit por trial.
- CIP-P3-8: `git rm` `colorStimulusLayout.js` + test (0 imports — verificado grep).
- Contrato `game_end`: presente en los 6 tasks stable_dg (verificado B.1; re-verificado aquí: `game_end` con score 0-100 + difficulty `color_interference` + test).
- Privacidad: `trials` del `onComplete` eliminado por `stripForbidden` y re-validado por `validateFinalAssessmentPayload` (recursivo); suite focal incluye `postulationDemoSessionBuilder` (7/7) y `finalAssessmentPayload` (2/2); smoke aserta payload de eventos sin `samples`.
- Smoke: `node scripts/smoke-b4-color-interference-2026-09-12.mjs` → `{failures: [], consoleErrors: [], ok: true}` (2026-09-12, Vite dev 127.0.0.1:5173, chromium-1234 headless, Pi libre).
- Contraste (note c): cálculo WCAG sobre `--k-card-cream` #f2e8dc — rojo 3.99 / azul 4.27 / verde 3.11 / amarillo 4.15 (AA large ≥3:1 PASS); pairs de UI: buttons 11.12, title 11.79, prompt 6.05, caption 6.02, timer 6.84, feedback 8.41/8.71.
